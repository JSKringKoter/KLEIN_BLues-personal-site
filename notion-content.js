window.NOTION_NOTE_CONTENT = {
  "Windows使用WSL访问ext4文件系统": "在去甲方公司部署的时候遇到需要从Windows电脑上拷贝文件到Linux系统上，但是移动磁盘是ext4文件系统的问题，windows系统无法识别ext4文件系统，导致无法找到磁盘。\n巨硬公司推出了一个服务可以帮助windows系统访问linux文件系统，即WSL服务。\n# 安装WSL\n## 1. 启用虚拟机\n安装wsl的前提是机器中需要有一台linux虚拟机（不能是VMWare虚拟机，而是Windows提供的虚拟机），需要在设置中开启Windows的虚拟机平台。**设置完成后需要重启。**\n![](assets/images/notes/wsl-ext4.png)\n## 2. 安装WSL\n使用**管理员身份**打开PowerShell，输入：\n```powershell\nwsl --install\n```\n等待ubuntu虚拟机安装完成，会出现需要创建账户，输入用户密码的提示。等待片刻即可安装完成。\n## 3. 使用WSL\n- 以管理员身份重新启动PowerShell，获取待加载的磁盘信息。\n```powershell\nGET-CimInstance -query \"SELECT * from Win32_DiskDrive\"\n```\n得到如下的磁盘列表：\n```powershell\n\nDeviceID           Caption                              Partitions Size          Model\n--------           -------                              ---------- ----          -----\n\\\\.\\PHYSICALDRIVE2 WD My Passport 264F SCSI Disk Device 1          1000169372160 WD My Passport 264F SCSI Disk Device\n\\\\.\\PHYSICALDRIVE1 Micron MTFDKBA512TFH                 4          512105932800  Micron MTFDKBA512TFH\n\\\\.\\PHYSICALDRIVE0 KINGBANK KP230 Pro                   1          1024203640320 KINGBANK KP230 Pro\n```\n- 找到磁盘对应的`DeviceID`，例如此处是`\\\\.\\PHYSICALDRIVE2` ，输入：\n```powershell\nwsl.exe --mount \\\\.\\PHYSICALDRIVE2 --bare\n```\n如果显示**操作成功完成**，那么我们知道已经成功将磁盘挂载到了wsl环境下面了，接下去可以由wsl中的linux进行加载了。\n- 进入wsl的命令行环境\n```powershell\nwsl\n```\n- 查看设备列表\n```powershell\nlsblk\n```\n- 在wsl命令行下面用mount命令挂载磁盘\n```powershell\nmount -t ext4 /dev/sdd1  /data\n```\n然后就可以在windows文件资源管理器下的Linux-ubuntu中访问这个路径，以及磁盘中的内容了。\n## 4. 卸载WSL以及磁盘\n- 卸载磁盘\n```powershell\numount /data\n```\n- 卸载WSL\n```powershell\nwsl.exe --shutdown\n```\n然后就可以安全地移除物理磁盘了。",
  "部署备忘录": "**2025.5.22周四 入职公司，要求将现有的Docker-compose部署方式换成Ansible方式。周四到周五完成了Ansible的打包，但是后来又要求使用Docker部署。**\n**2025.5.26周一 熟悉了Docker、Docker-compose和Linux的相关命令，在公司环境下实际操作了一遍部署流程。**\n**2025.5.27周二 实际去甲方公司部署。全程没有敲过命令，甲方公司的人完成了整个部署流程……甲方找到了不少bug，提出了一些新的需求，这是下一阶段的工作任务。**\n\n**不知道是幸运还是不幸，总之整个流程没我什么事情。**\n## 实际部署流程（可能）\n```shell\ntar -xf bz2.tar.gz          # 解压文件\ncd bz2                      # 进入bz2\ndocker load < bz2-base.tar  # 加载本地镜像\ndocker-compose up -d        # 启动docker compose服务\n```\n## 关于llm目录\n这个是用来更改甲方公司原有模型的docker服务的，需要注意甲方公司的模型需要放在\n```shell\n/mnt/HDD/model/qwen3-14b/model.bin\n```\n下。需要使用`docker-compose ps`查找原来的服务并关闭，然后重新启动这个新的服务。\n## 关于external目录\n如果甲方给出了新的文件，需要将这个文件夹里原来的文件替换掉。 可能需要现场改，可能需要使用Vim。\n## 磁盘挂载\n**可能需要先取消ubuntu自动的磁盘挂载。**\n外部设备必须挂载在根目录下的/mnt文件夹下。\n- 挂载流程\n```shell\nsudo fdisk -l                   # 列出磁盘列表\nsudo lsblk                      # 列出磁盘具体分区\nsudo mkdir /mnt/mydev           # 在/mnt下创建挂载点\nsudo mount /dev/sdc1 /mnt/mydev # 将磁盘挂载到/mnt下的挂载点下\nsudo df -h                      # 查看是否挂载成功\nsudo umount /mnt/mydev          # 取消挂载（弹出设备）\n```\n- 进程清理\n```shell\nsudo lsof +D /mnt/mydev         # 查看占用挂载点的进程\nsudo fuser -mf /mnt/mydev       # 终止这些进程\n```\n## Docker\n### 镜像相关\n```shell\ndocker images                     # 查看本地镜像\ndocker pull image_name[:tag]      # 拉取镜像\ndocker build -t image_name .      # 从dockerfile构建镜像\ndocker build -f /path/to/dockerfile -t image_name .\ndocker rmi image_name             # 删除镜像\ndocker load < app.tar             # 从本机加载镜像\ndocker save -o filename.tar imagename:base    # 保存镜像\n```\n### 容器操作\n```shell\ndocker ps           # 查看运行中的容器\ndocker ps -a        # 查看所有容器\ndocker run -d 8088:80 image_name    # 后台运行容器并映射端口\ndocker run -it image_name /bin/bash # 前台运行容器并交互式运行\ndocker stop container_id            # 停止容器\ndocker start container_id           # 启动容器\ndocker rm container_id              # 移除容器\n\ndocker exec -it container_id /bin/bash # 进入运行中的容器\n```\n### 日志\n```shell\ndocker logs container_id          # 查看容器内部日志\n```\n### 权限处理\n```shell\nsudo usermod -aG docker $USER     # 将当前用户加入docker组\nnewgrp docker                     # 应用组更改\n```\n\n## Docker-Compose\n```shell\ndocker-compose up # 前台启动\ndocker-compose up -d # 后台启动\ndocker-compose down  # 停止并清理容器、网络\ndocker-compose restart # 重启容器\ndocker-compose ps    # 列出所有服务\ndocker-compose logs  # 查看所有服务日志\ndocker-compose exec service_name bash # 进入某个服务\n```\n\n## 权限相关\n- 查看权限\n```shell\nls -l\n```\n- 修改权限\n```shell\nchmod 权限 文件/目录\n```\n<table>\n<tr>\n<td>符号</td>\n<td>含义</td>\n<td>符号</td>\n<td>含义</td>\n</tr>\n<tr>\n<td>u</td>\n<td>user</td>\n<td>w</td>\n<td>写</td>\n</tr>\n<tr>\n<td>g</td>\n<td>group</td>\n<td>r</td>\n<td>读</td>\n</tr>\n<tr>\n<td>o</td>\n<td>other</td>\n<td>x</td>\n<td>执行</td>\n</tr>\n<tr>\n<td>a</td>\n<td>all</td>\n<td></td>\n<td></td>\n</tr>\n</table>\n\n## 基本命令\n### 文件和目录\n```shell\ntouch a.txt     # 创建新文件\nmkdir dir       # 创建目录\nmkdir -p /a/b/c # 递归创建目录\n```\n### 复制和移动\n```shell\ncp a.txt b.txt  # 复制\nmv a.txt b.txt  # 重命名\nmv a.txt /dir/  # 移动\n```\n### 查看内容\n```shell\ncat a.txt        # 查看文件内容\n```\n### 解压与压缩\n```shell\ntar -xf filename.tar.gz    # 解压\ntar -cf filename.tar.gz filedir/   # 压缩\n```\n### 辅助命令\n```shell\npwd    # 查看当前路径\ncd     # 切换\n```",
  "LLaMA-Factory实现大模型微调": "# 关于微调\n## 什么是大模型微调\n大模型微调（Fine-tuning）是一种通过在预训练模型基础上使用特定任务的数据进行进一步训练的技术。这个过程能够让通用大语言模型更好地适应特定领域或任务的需求，提高模型在目标场景下的表现。\n微调过程通常包括准备领域特定数据、选择合适的训练参数、使用较小的学习率对模型进行训练等步骤。相比于从头训练模型，微调能够大大减少计算资源的消耗，同时保持模型的基础能力。\n## 为什么需要微调大模型\n- 提高模型对企业专有信息的理解、增强模型在特定行业领域的知识。\n- 提供个性化和互动性强的学习\n## **SFT、RLHF、RAG**\n- SFT**（Supervised Fine-Tuning）有监督微调**\n\t通过提供人工标注的数据，进一步训练**预训练模型**，让模型能够更加精准地处理特定领域的任务\n- **RLHF（Reinforcement Learning from Human Feedback）强化学习**\n\t- DPO（Direct Preference Optimization）<br>核心思想：通过**人类对比选择**（例如：A 选项和 B 选项，哪个更好）直接优化生成模型，使其产生更符合用户需求的结果；调整幅度大。\n\t- PPO（Proximal Policy Optimization）\n\t\t核心思想：通过 **奖励信号**（如点赞、点踩）来 **渐进式调整模型的行为策略**；调整幅度小。\n- **RAG（Retrieval-Augmented Generation）检索增强生成**\n\t将外部信息检索与文本生成结合，帮助模型在生成答案时，实时获取外部信息和最新信息\n## 选择微调还是RAG？\n- 微调：\n\t- 适合：拥有非常充足的数据\n\t- 能够直接提升模型的固有能力；无需依赖外部检索；\n- RAG:\n\t- 适合：只有非常非常少的数据；动态更新的数据\n\t- 每次回答问题前需耗时检索知识库；回答质量依赖于检索系统的质量；\n- 总结：\n\t- 少量企业私有知识：最好微调和 RAG 都做；资源不足时优先 RAG；\n\t- 会动态更新的知识：RAG\n\t- 大量垂直领域知识：微调\n# 使用LLama-Factory在Window系统上实现微调\n1. **安装部署LLama-Factory**\nLLaMA-Factory 的 Github地址：[https://github.com/hiyouga/LLaMA-Factory](https://github.com/hiyouga/LLaMA-Factory)\n- 克隆仓库\n```bash\ngit clone --depth 1 https://github.com/hiyouga/LLaMA-Factory.git\n```\n- 创建conda虚拟环境（需使用3.10的python版本，不然不兼容）\n```bash\nconda create -n llama-factory python=3.10\n```\n- 初始化cmd终端\n在Window使用anaconda虚拟环境时需要先激活才能使用。\n在管理员权限下执行：\n```bash\nconda init\n```\n- 激活虚拟环境\n```bash\nconda activate llama-factory\n```\n- 在虚拟环境中安装相关依赖\n```bash\npip install -e \".[torch,metrics]\"\n```\n- 检查安装是否成功\n```bash\nllamafactory-cli version\n```\n- 启动可视化界面\n```bash\nllamafactory-cli webui\n```\n1. **从HuggingFace下载基座模型**\n- 安装huggingface下载工具\n```bash\npip install -U huggingface_hub\n```\n- 下载基座模型\n```bash\nhuggingface-cli download --resume-download deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B\n```\n1. **准备用于训练的数据集**\n训练集是一个json格式的数据，包括instruction、input和output。\n- **instruction（指令）**：描述任务要求或问题，告诉模型应该做什么，比如\"翻译以下文本\"、\"总结下面的内容\"等。\n- **input（输入）**：需要模型处理的具体内容。某些任务可能不需要input，这种情况下input字段可以为空。\n- **output（输出）**：期望模型产生的结果，用作训练时的标准答案。\n例如：\n```json\n[\n  {\n    \"instruction\": \"请问你是谁\",\n    \"input\": \"\",\n    \"output\": \"您好，我是蟹堡王的神奇海螺，很高兴为您服务！我可以回答关于蟹堡王和汉堡制作的任何问题，您有什么需要帮助的吗？\"\n  },\n  {\n    \"instruction\": \"怎么修复这个报错\",\n    \"input\": \"我正在使用蟹堡王全自动智能汉堡制作机，报错信息是：汉堡食谱为空\",\n    \"output\": \"根据您提供的错误信息，'汉堡食谱为空' 可能是因为系统没有加载正确的食谱文件或食谱文件被删除。您可以尝试以下步骤：\\n1. 检查食谱文件是否存在，并确保文件路径正确。\\n2. 重新加载或更新食谱文件。\\n3. 如果问题依然存在，尝试重启机器并检查是否有软件更新。\\n希望这些步骤能帮助您修复问题。如果仍有困难，请与蟹堡王技术支持联系。\"\n  }\n]\n```\n修改**dataset_info.json** 文件，添加数据集配置：\n```json\n\"magic_conch\": {\n\"file_name\": \"magic_conch.json\"\n},\n```\n将数据集 magic_conch.json 放到 LLama-Factory 的 **data 目录** 下。\n1. **在页面上进行微调的相关设置，开始微调**\n- 选择微调算法 **Lora**\n- 添加数据集 **magic_conch**\n- 修改其他训练相关参数，如学习率、训练轮数、截断长度、验证集比例等\n\t- 学习率（Learning Rate）：决定了模型每次更新时权重改变的幅度。过大可能会错过最优解；过小会学得很慢或陷入局部最优解\n\t- 训练轮数（Epochs）：太少模型会欠拟合（没学好），太大会过拟合（学过头了）\n\t- 最大梯度范数（Max Gradient Norm）：当梯度的值超过这个范围时会被截断，防止梯度爆炸现象\n\t- 最大样本数（Max Samples）：每轮训练中最多使用的样本数\n\t- 计算类型（Computation Type）：在训练时使用的数据类型，常见的有 float32 和 float16。在性能和精度之间找平衡\n\t- 截断长度（Truncation Length）：处理长文本时如果太长超过这个阈值的部分会被截断掉，避免内存溢出\n\t- 批处理大小（Batch Size）：由于内存限制，每轮训练我们要将训练集数据分批次送进去，这个批次大小就是 Batch Size\n\t- 梯度累积（Gradient Accumulation）：默认情况下模型会在每个 batch 处理完后进行一次更新一个参数，但你可以通过设置这个梯度累计，让他直到处理完多个小批次的数据后才进行一次更新\n\t- 验证集比例（Validation Set Proportion）：数据集分为训练集和验证集两个部分，训练集用来学习训练，验证集用来验证学习效果如何\n\t- 学习率调节器（Learning Rate Scheduler）：在训练的过程中帮你自动调整优化学习率\n- 页面上点击**启动训练**，或复制命令到终端启动训练\n\t- 实践中推荐用 `nohup` 命令将训练任务放到后台执行，这样即使关闭终端任务也会继续运行。同时将日志重定向到文件中保存下来\n- 在训练过程中注意观察损失曲线，**尽可能将损失降到最低**\n\t- 如损失降低太慢，尝试增大学习率\n\t- 如训练结束损失还呈下降趋势，增大训练轮数确保拟合\n1. **微调结束，评估微调效果**\n- 观察损失曲线的变化；观察最终损失\n- 在交互页面上通过预测/对话等方式测试微调好的效果\n- **检查点**：保存的是模型在训练过程中的一个中间状态，包含了模型权重、训练过程中使用的配置（如学习率、批次大小）等信息，对LoRA来说，检查点包含了**训练得到的 B 和 A 这两个低秩矩阵的权重**\n- 若微调效果不理想，可以：\n\t- 使用更强的预训练模型\n\t- 增加数据量\n\t- 优化数据质量（数据清洗、数据增强等，可学习相关论文如何实现）\n\t- 调整训练参数，如学习率、训练轮数、优化器、批次大小等等\n### **11. 导出合并后的模型**\n- 为什么要合并：因为 LoRA 只是通过**低秩矩阵**调整原始模型的部分权重，而**不直接修改原模型的权重**。合并步骤将 LoRA 权重与原始模型权重融合生成一个完整的模型\n- 先创建目录，用于存放导出后的模型\n```bash\nmkdir -p Models/deepseek-r1-1.5b-merged\n```\n- 在页面上配置导出路径，导出即可。",
  "Python-FastAPI": "# 相关知识\n## 异步编程\n异步编程是一种处理并发操作的方法，使得程序可以在等待耗时操作（如文件读写、网络请求或数据库查询），完成的同时继续执行其他任务。与同步编程不同，异步编程不要求程序按顺序等待每个操作完成，而是通过回调、Promise或者async/await等机制，在操作完成时再执行特定的逻辑。\n通俗的讲，异步编程即是允许我们在执行一个长时间的任务时，程序不需要等待，而是继续执行之后的代码，知道这些任务完成之后再回来通知你，通常是以回调函数(callback)的方式。这种编程模式避免了程序的堵塞，大大提高了cpu的执行效率。尤其适合一些io密集的操作。例如需要经常进行网络操作，数据库访问的应用。\n### 回调函数（Callback Function）\n回调函数是完成一个异步操作后执行的操作。例如在网络读写成功后，将会调用一个函数用于处理响应数据，这个函数称为回调函数。\n### Promise\nPromise是一个对象，用来表示一个异步操作完成或失败后的结果值。Promise具有三种状态：pending(进行中)，fulfilled(已成功)和rejected(已失败)。Promise通过`then`、 `catch`和`finally`方法来处理异步操作的结果。\n例如，我们在编写前端操作时，通常会使用Fetch库，这是一个使用Promise接口的库：\n```javascript\nfetch(\"jskring.top\").then((response) => {\n    response.json();\n});\n```\nfetch函数的返回值是一个Promise对象，它记录了网络操作的结果，如果请求成功完成，那么回调函数会被调起。请求的结果也会以参数的形式传递进来。而且，调用then函数后的返回值依然是一个Promise，也就是说可以这样将多个then函数或者catch和finally函数串联起来：\n```javascript\nfetch(\"jskring.top\").then((response) => {\n    response.json();\n}).then((json) => {\n    console.log(json);\n}).catch((err) => {\n    console.log(err);\n}).finally(() => {\n    console.log(\"请求完成\");\n})\n```\n### async/await\nasync/await是Promise的语法糖，它可以让异步操作写起来像同步操作一样。\n`async/await`函数总是返回一个Promise，`await`用于等待Promise完成，并 返回Promise的结果。它使得代码更易读，更易维护。但是需要注意的是：await看上去是需要暂停函数的执行，但是在等待过程中，js同样可以处理其他任务。比如更新界面，运行其他程序代码等等。因为await底层也是基于Promise和事件循环机制实现的。\n使用：\n- 首先我们需要将将async关键字将函数标记为异步函数（异步函数指的是返回值为Promise对象的函数），比如之前用到的fetch()就是一个异步函数。\n- 在异步函数中，我们可以调用其他异步函数，不过我们不需要使用then()，而是使用一个更加简洁的await语法，await会等待Promise完成之后直接返回最终的结果。\n\n## 安装并进入.venv虚拟Python环境\npython的环境配置一直是一个头疼的问题。两个不同的项目可能需要不同的依赖版本，这可能会引起冲突。所以，为每一个项目单独构建一虚拟的python环境是非常有必要的。\n### 创建venv\n你可以运行\n```shell\npython -m venv [venv_name]\n```\n来创建一个虚拟环境，但是也可以在vscode中按下CTRL+SHIFT+P后输入python create environment找到创建python环境，用vscode自动创建一个名为.venv的环境。这样会更加规范。\n### 进入venv\n在项目根目录下运行\n```shell\n.venv/Scripts/activate\n```\n即可进入虚拟环境，成功的标志是命令行前出现`(.venv)` 。然后你就可以在这个环境下安装依赖（例如FastAPI）了。\n\n# FastAPI\nFastAPI 是一个轻量级、用于构建 API 的现代、快速（高性能）的 web 框架，使用 Python 并基于标准的 Python 类型提示。\n## 多文件项目结构\nfastapi项目以app.py为入口文件，只能在这里面使用app注解，可以通过引入其他文件夹下的包来区分功能。\n项目格式如下：\n```shell\n.\n├── app                  # 「app」是一个 Python 包\n│   ├── __init__.py      # 这个文件使「app」成为一个 Python 包\n│   ├── main.py          # 「main」模块，例如 import app.main\n│   ├── dependencies.py  # 「dependencies」模块，例如 import app.dependencies\n│   └── routers          # 「routers」是一个「Python 子包」\n│   │   ├── __init__.py  # 使「routers」成为一个「Python 子包」\n│   │   ├── items.py     # 「items」子模块，例如 import app.routers.items\n│   │   └── users.py     # 「users」子模块，例如 import app.routers.users\n│   └── internal         # 「internal」是一个「Python 子包」\n│       ├── __init__.py  # 使「internal」成为一个「Python 子包」\n│       └── admin.py     # 「admin」子模块，例如 import app.internal.admin\n```\n__init__.py作为一个包的标示，在第一次运行时需要创建，之后就不需要了（存疑）\n## 连接MySql\nfastapi连接MySql真是个麻烦的事情，就这方面比mybatis麻烦很多。但是把准备工作做好了写起来很舒服，和mybatis-plus类似，不需要手动写sql了。\n### 安装SQLModel和sql-connector-python\nsql-connector-python是python连接数据库的驱动，SQLModel是FastAPI官方的库，可以很方便地创建数据库映射类和数据库连接。使用pip install即可安装。\n### 创建引擎（Engine）\nSQLModel的Engine是一个用来与数据库保持连接的对象。\n使用这两行代码创建引擎：\n```python\nDATABASE_URL = \"mysql+mysqlconnector://username:password@localhost:3306/database_name\"\nengine = create_engine(DATABASE_URL)\n```\n### 创建会话（Session）依赖项\n会话是程序与数据库交互的对象。它可以暂存程序中的数据对象，然后更新数据库；亦可以查询数据库，然后将结果储存在程序中的数据对象了里。\n我们使用yield为每一个请求提供一个新的Session，这样确保每一个请求都使用一个单独的Session。\n```python\ndef get_session():\n    with Session(engine) as session:\n        yield session\n\nSessionDep = Annotated[Session, Depends(get_session)]\n```\n> with-yield块可以使得函数传出一个资源，然后在这个资源执行完后对其销毁。\n> Annotated是python自带的一个Typing，它可以向一个类型的注解中增加元数据\n我们将上面的两个代码块放在同一个文件当中，命名为db.py，这个文件用来连接数据库。\n### 创建模型\n模型就像Springboot中的实体类，但是在这里需要指明字段的一些属性，比如默认值，是否是主键、外键、索引等等。这些指定通过sqlmodel的Field函数进行。\n```python\nclass Drink(SQLModel, table=True):\n\t\t__tablename__ = \"drink\"\n    id: int | None = Field(default=None, primary_key=True)\n    name: str\n    description: str | None = None\n    price: float\n```\ndefault字段指定了为空时的默认值，primary_key说明这是一个主键。table=True表明这是一个<span color=\"orange\">直接对接数据库</span>的实体类。它应该表示 SQL 数据库中的一个*表*，而不仅仅是一个*数据模型。*\n默认情况下，表名应该是模型名字的小写。但是你可以通过__tablename__字段手动指定绑定的表名。\n### 实现增删改查操作\n如果需要实现数据库操作，需要在router后面定义的函数参数中加上session参量，而且使用SessionDep构造。\n1. 查\n\t```python\n@router.get(\"/meal/listAllMeal\")\nasync def listAllMeal(session: SessionDep) -> list[Meal]:\n# select(Meal)使用 select 函数构建查询语句，这里表示查询所有的 Meal 对象\n# 使用 session.exec() 执行查询语句，返回一个结果生成器\n# 使用 all() 方法将结果生成器中的所有结果转换为列表\n    meals = session.exec(select(Meal)).all()\n    return meals\n\n@router.get(\"/meal/{mealid}\")\nasync def getMealById(mealid: int, session: SessionDep) -> Meal:\n# 表示查询id为mealid的Meal对象\n    meal = session.get(Meal, mealid)\n    if not meal:\n        raise HTTPException(status_code=404, detail=\"Meal not found\")\n    return meal\n\t```\n\t上面两个函数分别实现了列表和id查询的操作。\n2. 增\n\t```python\n@router.post(\"/meal/addMeal\")\nasync def addMeal(meal: Meal, session: SessionDep):\n    session.add(meal)\n    session.commit()\n    session.refresh(meal)\n    return f\"add success:{meal}\"\n\t```\n\t这个很简单，只需要调用session.add()函数就可以了。commit()函数用来将更改推送至数据库，refresh()函数用来同步数据库与程序中的更改。\n3. 删\n\t```python\n@router.delete(\"/meal/{mealid}\")\nasync def deleteMealById(mealid: int, session: SessionDep):\n    meal = session.get(Meal, mealid)\n    if not meal:\n        raise HTTPException(status_code=404, detail=\"Meal not found\")\n    session.delete(meal)\n    session.commit()\n    return \"delete success\"\n\t```\n\t进行删除操作时，需要先使用一次查操作获得需要删除的对象，并检验改对象是否存在。如果该对象存在，则将它传给delete()函数，并commit更改。\n4. 改\n\t```python\n@router.post(\"/meal/update\")\nasync def update(updateMeal: Meal, session: SessionDep):\n    meal = session.get(Meal, updateMeal.id)\n    if not meal:\n        raise HTTPException(status_code=404, detail=\"Meal not found\")\n    # 替换掉数据库对象的字段数据\n    mealData = updateMeal.dict(exclude_unset=True)\n    for key, value in mealData.items():\n        setattr(meal, key, value)\n    session.add(meal)\n    session.commit()\n    session.refresh(meal)\n    return \"update success\"\n\t```\n\t改部分的逻辑比较复杂。\n\t第一步：通过前端传来的对象获取需要更新的id，通过id获得需要更新的行的<span color=\"orange\">引用</span>。需要注意，session.get()获得的是数据库对象的引用，也就是说，对这个对象的修改在commit后会直接作用与这个数据库对象。\n\t第二步：替换掉meal中需要更新的字段。\n\t第三步：将对meal引用的更改commit到数据库中。\n\t如果这里不获取meal，直接把updateMeal add到数据库，会报错，因为这相当于增加了一个新的行。必须获取数据库对象后，对该对象修改并commit。",
  "RAG & RAGFlow知识库构建": "设想一个场景，咖啡屋的老板让你作为客服回答客户的问题，你非常机灵地将可能遇到的问题做成了一份文档，并编写了一个使用DeepSeek接口的AI对话软件。你在每轮对话开始时都将文件内容作为上下文上传了上去，这样当客户问及有关问题时，AI会根据文件内容做出准确的回答。一个星期过去了，咖啡屋生意火爆，咨询菜单等问题的人非常多，你突然发现，老板给你的200块token费用竟然全部用光了！老板非常生气，说要给你扣工资。就在这时候，你发现了另一项技术：RAG。\n# 什么是RAG\nRAG全称Retrieval-Augmented Generation，译为检索增强生成。它将文件通过嵌入模型（Embedding模型）切片并转换成向量，内容相近的两段文字在向量空间中会非常接近，这样就可以通过检索将获得文件中有关的内容，而不用将整个文件上传到LLM中。这样的向量空间称为知识库，LLM可以通过向知识库检索获得所需要的知识。\n# RAGFlow\nRagFlow是一款集成了Rag、知识库和AI Agent开发的软件，它使用Docker部署。可以很方便地部署在本地并为后端暴露端口。\n## RAGFlow安装\n官方文档如下：[https://github.com/infiniflow/ragflow/blob/main/README_zh.md](https://github.com/infiniflow/ragflow/blob/main/README_zh.md)\n简易的安装方法如下：\n- 克隆仓库\n\t```shell\ngit clone https://github.com/infiniflow/ragflow.git\n\t```\n- 修改配置\n\t进入docker文件夹，打开其中的.env文件，将其中的配置修改为如下：\n\t```shell\n# 注释掉这一行\n# RAGFLOW_IMAGE=infiniflow/ragflow:v0.17.2-slim\n\n# 取消注释这一行\nRAGFLOW_IMAGE=infiniflow/ragflow:v0.17.2\n\t```\n\tragflow默认下载的版本是slim版本，这个版本是没有embedding模型的。\n- 启动项目\n\t在docker文件夹下运行\n\t```shell\n# 使用cpu运行模型\ndocker compose -f docker-compose.yml up -d\n\n# 使用gpu运行模型\ndocker compose -f docker-compose-gpu.yml up -d\n\t```\n\t然后应该就能在Docker Desktop中看到容器的运行情况了。如果出现以下界面则说明运行成功：\n\t```plain text\n     ____   ___    ______ ______ __\n    / __ \\ /   |  / ____// ____// /____  _      __\n   / /_/ // /| | / / __ / /_   / // __ \\| | /| / /\n  / _, _// ___ |/ /_/ // __/  / // /_/ /| |/ |/ /\n /_/ |_|/_/  |_|\\____//_/    /_/ \\____/ |__/|__/\n\n * Running on all addresses (0.0.0.0)\n\t```\n- 访问项目\n\t直接在浏览器访问localhost就好了，沟槽ragflow的开的是80端口。",
  "卷积神经网络": "卷积神经网络（Convolutional Neural Network，CNN）是一种在计算机视觉领域取得了巨大成功的深度学习模型。它们的设计灵感来自于生物学中的视觉系统，旨在模拟人类视觉处理的方式。在过去的几年中，CNN已经在图像识别、目标检测、图像生成和许多其他领域取得了显著的进展，成为了计算机视觉和深度学习研究的重要组成部分。\n## 一、什么是卷积\n卷积操作是将一个可移动的数据窗口与图像进行逐元素相乘的过程，这个小窗口是一组固定值的权重，它被称作Filter（滤波器或卷积核），它的作用是与图像逐元素相乘后得到对应的权重，提取出图像的特征。通过与图像不同区域的卷积操作，卷积核能总结出图像哪些地方是重要的，哪些地方不重要，以及不同的重要区域有哪些特征。\n下面是一个2卷积核，卷积核大小3\\*3\\*3，padding=1，stride=2的卷积操作过程：\n![](assets/images/notes/cnn-convolution.gif)\n卷积核在input图像上以step步长移动，每次移动做一次相乘，最终结果记录在output矩阵中。ouput矩阵即特征图。\n## 二、卷积的作用\n在传统神经网络中，计算机无法识别出一个特定的图像或形状。我们希望一个物体不管在画面左侧还是右侧，都会被识别为同一物体，这一特点就是不变性。为了实现平移不变性，卷积神经网络模型在卷积层中使用了卷积操作，这个操作可以捕捉到图像中的局部特征而不受其位置的影响。与传统神经网络相比，卷积神经网络拓展了数据的维度，降低了参数的复杂程度。它可以用来处理RGB彩色图像。通过卷积，可以很好地提取出图像的特征。\n![](assets/images/notes/cnn-comparison.png)\n## 三、卷积的各项参数\n- 滑动窗口步长 stride\n\t卷积核每次移动的距离，记为S\n- 卷积核尺寸\n\t卷积核的大小和维度。卷积核的维度要和input输入图像的维度一致。卷积核宽度记为Fw，长度记为Fh\n- 边缘填充 padding\n\t在原始输入图像外围填充0的层数。记为P\n- 卷积核个数\n\t有多少个卷积核。有几个卷积核得到的特征图就有几层。\n则卷积结果计算公式为：\n$$\nH_2 = \\frac{H_1 - F_H + 2P} {S} +1\n$$\n$$\nW_2 = \\frac{W_1 - F_W + 2P} {S} +1\n$$\n其中H1、W1为输入图像的长度和宽度，H2、W2为输出图像的长度和宽度\n## 四、边缘填充\n在卷积操作中，图像内部的像素点被带入卷积计算的次数更多，而边缘地区很少。例如，在一个3\\*3input，2\\*2卷积核，stride=1的卷积操作中，图像中心点像素被计算了3次，而四角像素只被计算了一次。所以，我们选择在图像的外围添加一圈0，以减小计算密度不平均对特征值造成的影响。\n## 五、池化\n在多次卷积操作后，得到的特征图经常会很大，特征值非常多。在这些特征值中，很多是相似的或者无用的。池化操作就是对特征图进行压缩（或者叫做下采样downsapling），最终目的是提取其中最重要的特征值，忽略不重要的，达到不损失精度的情况下简化的目的。\n![](assets/images/notes/cnn-pooling.png)\n池化有多种方法，最常用的是Max Pooling最大池化和Average Pooling平均池化。前者为取出每个下采样单位中最大的值，后者为对下采样单位中的每个值求平均数。经过学者论证，Max Pooling的效果普遍要比Average Pooling好。因为池化操作要保留每个单位中最明显的特征，平均操作会中和这个特征。\n在这次实验中，我均采用2\\*2 Filters，Stride=2的池化操作，这将会把特征图大小减小为原有的四分之一。\n## 六、Relu函数\nRelu函数是一种人工神经网络中常用的激活函数，通常意义下，其指代数学中的斜坡函数，即：\n$$\nF(x) = max(0, x)\n$$\n而在神经网络中，ReLU函数作为神经元的激活函数，为神经元在线性变换之后的非线性输出结果。换言之，对于进入神经元的来自上一层神经网络的输入向量x，使用ReLU函数的神经元会输出：\n$$\nmax(0, w^Tx + b)\n$$\n至下一层神经元或作为整个神经网络的输出（取决现神经元在网络结构中所处位置）。\n在卷积层之后，通常会应用一个Relu函数，以引入非线性特性，增强模型的表达能力。\n## 七、整体网络架构\n1. **输入层INPUT**：\n\t输入层接收原始图像数据。图像通常表示为三维量（高度、宽度、通道），例如彩色图像有三个通道（红、绿、蓝）。\n2. **卷积层CONV**：\n\t卷积层通过卷积操作提取图像的特征。卷积操作使用一组可学习的卷积核在输入图像上滑动，计算局部区域的加权和。每个卷积核生成一个特征图，表示输入图像在特定特征上的响应。\n3. **激活函数RELU**：\n\t在卷积层之后，应用Relu函数，增强模型的表达能力\n4. **池化层POOL**：\n\t池化层用于降低特征图的空间维度（高度和宽度），减少计算量，并增强模型的平移不变性。常见的池化操作包括最大池化和平均池化。\n5. **全连接层FC**：\n\t在经过多个卷积层和池化层之后，特征图被展平为一维向量，并输入到全连接层。全连接层中的每个神经元都与前一层的所有神经元相连，用于学习特征之间的复杂关系。\n6. **输出层OUTPUT**：\n\t输出层通常是一个全连接层，根据任务的不同，输出可以是分类任务的类别概率（如 Softmax 激活函数）或回归任务的连续值。\n通常来说，一次卷积操作并不能完全提取图像中的特征，这时就要考虑多次卷积操作。\n![](assets/images/notes/cnn-flow.png)\n## 八、经典网络VGG\nVGG网络是2014年提出的深度卷积神经网络，其主要特点是使用多个小尺寸（3x3）的卷积核堆叠来替代大尺寸的卷积核，从而增加网络的深度。VGG网络有多个版本，其中最著名的是VGG16和VGG19，分别包含16层和19层卷积层。这些网络在2014年的ImageNet挑战赛中表现出色，尤其在图像分类任务中取得了优异的成绩，成为深度学习领域的重要里程碑。\n在后续的实验中，人们发现VGG的19层layer已经是极限了，再增加layer数量，errer率不升反降，且训练时间成倍增长。原因是增加的layer不能保证一定是优秀的、有作用的，很多layer反而无法提取特征，拉高了errer率。于是人们提出了残差网络Resnet。\n## 九、残差网络Resnet\n残差网络是由微软研究院的何凯明等人提出的一种深度卷积神经网络架构，旨在解决深度神经网络训练中的梯度消失和梯度爆炸问题。其核心创新在于引入了“残差块”（Residual Block），通过跳跃连接（skip connection）将输入直接添加到网络的输出中，使得网络可以学习输入与输出之间的残差，而不是直接学习输出。这种设计使得网络能够更深，同时保持训练的稳定性，从而在图像分类、目标检测等任务中取得了显著的性能提升。ResNet在2015年的ImageNet挑战赛中大放异彩，标志着深度学习网络架构设计的一个重要突破。",
  "SpringCloud 分布式": "# Eureka 注册中心\n在微服务系统中，各个服务会动态地启动、停止或扩容，服务的地址和端口也可能随时变化。**Eureka** 提供一个“电话簿/注册中心”的角色。\n> 生产者：服务提供者<br>消费者：服务使用者\n每个生产者或消费者启动时，会将地址和元数据在Eureka注册。\n当其他服务想要调用时就会查询Eureka，不需要写死端口和地址。服务会定期向Eureka发送心跳以检测可用性。\n## 配置Eureka服务端\n- 创建Eureka项目并引用Eureka依赖\n\t```xml\n<dependencies>\n        <dependency>\n            <groupId>org.springframework.cloud</groupId>\n            <artifactId>spring-cloud-starter-netflix-eureka-server</artifactId>\n        </dependency>\n </dependencies>\n\t```\n- 配置Eureka\n\tEureka本身就是一个Eureka服务，它需要在Euerka中注册，所以既需要指定服务端口也需要指定在eureka中注册的端口和地址。\n\t```yaml\nserver:\n  port: 10086\n\nspring:\n  application:\n    name: eureka-server\neureka:\n  client:\n    service-url:\n      defaultZone: http://127.0.0.1:10086/eureka\n\t```\n## 配置Eureka客户端\n- 创建Eureka项目并引用Eureka依赖（注意这里是client）\n\t```xml\n<dependencies>\n        <dependency>\n            <groupId>org.springframework.cloud</groupId>\n            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>\n        </dependency>\n </dependencies>\n\t```\n- 配置Eurek\n\t```yaml\nspring:\n  application:\n    name: order-server\neureka:\n  client:\n    service-url:\n      defaultZone: http://127.0.0.1:10086/eureka\n\t```\n## 服务发现\n- 修改访问路径，用服务名代替ip和端口：\n\t```java\nString url = \"http://servername/user\"\n\t```\n- 在RestTemplate上增加负载均衡注解，使得它可以解析上面的url为一个实例列表。\n\t```java\n@Bean\n@LoadBalanced\npublic RestTemplate restTemplate() {\n\treturn new RestTemplate();\n}\n\t```\n# Ribbon 负载均衡\nRibbon是一个**客户端**负载均衡器，它用于解决服务调用时的负载均衡问题。\n它和传统的服务端负载均衡不同，它将负载均衡放在客户端。在调用服务之前，会从注册中心（例如Eureka）中获取到服务的多个实例列表，然后按策略选择一个示例发起请求。\nRibbon有多种均衡策略，例如RoundRobnRule（轮询机制），RandomRule（随机机制）等。上面提到的RoadBalance就是Ribbon的一个注解。\n# Nacos 注册中心\n**Nacos** 是阿里巴巴开源的一个 **服务发现与配置管理平台**，类似于Eureka。\n## 服务分级存储模型\nNacos将实例按照地域划分成了集群，例如本地集群，南京集群等。由于跨集群调用服务延迟较高，有必要将每个集群配置调用优先级。\n```yaml\nspring:\n\tcloud:\n\t\tnacos:\n\t\t\tserver-addr: localhost:8848\n\t\t\tdiscovery:\n\t\t\t\tcluster-name: 南京\n```\n## NacosRule 负载均衡\nNacos集成了许多负载均衡策略，例如NacosRule，它会优先寻找与自己同集群的服务。\n```yaml\nuserservice:\n\tribbon:\n\t\tNFLoadBalancerRuleClassName: com.alibaba.cloud.nacos.ribbon.NacosRule\n```\n同样，Nacos还能单独配置每一个地域实例的权重，权重高的实例将被优先访问。如果权重为0，则完全不会被访问。\n## Nacos 环境隔离\n可以配置实例的namespace，可以将两个实例配置为不同namespace，不同的namespace中的实例无法访问。每个namespace都有唯一的id，配置时配置的也是id而非名称。\n## Nacos和Eureka的区别\n- 对于生产者：当生产者挂掉之后，Eureka会立即从注册中心中删除这一实例，而Nacos会将其标记为不健康，并等待其恢复，并不会直接将其删除。\n- 对于消费者：消费者每隔一段时间会向注册中心请求实例列表，Eureka仅会被动接受请求，而Nacos会在服务列表有更新时主动向消费者推送。\n# Nacos 配置管理\nNacos可以用于配置管理，可以将一些经常会更改的配置交给Nacos管理，当这些配置有更新时，Nacos会通知服务更新配置。例如，可以将日期格式配置交由Nacos管理：\n```yaml\npattern:\n\tdateformat: yyyy-MM-dd HH:mm:ss\n```\n在程序启动过程中，需要先获取到Nacos的地址才能进行配置管理，但是引入Nacos后，会先从Nacos获取配置，再获取application.yml的配置，这是程序并不知道Nacos的地址是什么。\n所以，需要在微服务中添加bootstrap.yml文件，用于配置Nacos地址等信息。这个文件的加载优先级较高，会在获取Nacos配置之前加载。\n# Gateway 网关\n在生产环境中，不可能将微服务的各个实例直接暴露给外部用户，所以需要网关来实现转发。Spring提供了两个网关gateway和zuul，前者基于Springflux，为响应式结构，较新；后者基于阻塞式编程，是较为陈旧的技术。\n使用网关需要编写路由配置和nacos地址。网关也作为一种可被发现的nacos服务，所以需要在nacos中注册。\n```yaml\nserver:\n\tport: 10010 # 网关端口\nspring:\n\tapplication:\n\t\tname: gateway  # 服务名称\n\tcloud:\n\t\tnacos:\n\t\t\tserver-addr: localhost:8848  # nacos地址\n\t\tgateway:\n\t\t\troutes:  \n\t\t\t\t- id: user-service  # 网关id，需要唯一\n\t\t\t\t\turi: lb://userservice  # 路由的目标地址 lb为负载均衡，后面为服务名称\n\t\t\t\t\tpredicaties:  # 路由断言，判断请求是否符合路由规则的条件\n\t\t\t\t\t\t- Path=/user/**   # 按照路径匹配，以/user/开头就符合要求 \n```\n## 路由断言工厂\n在配置文件中写明的断言规则是字符串，这些字符串会被断言工厂所读取，然后转换为真正的判断条件。\n路由断言工厂提供了11种不同的判断条件。\n## 过滤器配置\n过滤器可以对经过路由的请求或响应做加工处理，比如添加请求头。配置在路由下的过滤器只对当前路由的请求生效。也可以使用defaultFilters添加对所有路由都生效的过滤器。\n```yaml\ngateway:\n\trouters:\n\t\t- id: user-service\n\t\t\turi: lb://userservice\n\t\t\tfilters: # 单路由过滤器\n\t\t\t\tAddRequestHeader=Cappuccino\t\n\tdefault-filters: # 默认过滤器（针对所有路由）\n\t\t- AddRequestHeader=Cappuccino\t\n```\n## 全局过滤器\n全局过滤器可以实现自定义的过滤器。它不同于默认过滤器。默认过滤器只能使用几种定义好的逻辑，而全局过滤器可以自行编写代码逻辑。\n想要实现全局过滤器，需要实现GlobalFilter接口。\n```java\npublic class AuthorizeFilter implements GlobalFilter {\n\tpublic Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {\n\t\tServerHttpRequest request = exchange.getRequest();\n\t\tMultiValueMap<String, String> params = request.getQueryParams();\n\t\tString auth = params.getFirst(\"authorization\");\n\t\tif (\"admin\".equals(auth)) {\n\t\t\t//放行\n\t\t\treturn chain.filter(exchange);\n\t\t}\n\t\texchange.getResponse().setStatusCode(401);\n\t\t//拦截请求\n\t\treturn exchange.getResponse().setComplete();\n\t}\n}\n```\n# RabbitMQ",
  "RabbitMQ": "",
  "字节跳动面试速查表": "## Redis\n---\n### Redis有哪些数据结构\nString、Hash、Set、ZSet、List\n新增的数据结构：BitMap、HyperLogLog、GEO、Stream\n### Zset使用了什么数据结构\n压缩链表或者跳表，但是压缩链表已经被弃用\n### 什么是SkipList\nSkipList维护了多个层级的头指针。如果要查找一个元素，可以先跳到最近的一个元素，然后再开始遍历。这样将时间复杂度从N变成了logN\n它的空间复杂度是N\n### 为什么MySql不用SkipList\n因为B+树在三层时数据量可能已经到千万级别，如果使用跳表会导致跳表层数过高，导致磁盘io次数增多。\n### Redis为什么性能好\n- Redis的大部分操作都在内存当中，且采用了高效的数据结构。\n- Redis采用单线程模型，避免了多线程间的竞争，减少了线程切换的开销。\n- Redis采用了I/O多路复用机制。该机制允许内核存在多个监听Socket和已连接Socket。内核会不断监听，一旦有请求到达，就会交给Redis处理。\n### Redis和MySql如何保持一致性\n先更新数据库，再删除缓存\n## MySQL\n---\n### 事务有哪些特性\n- 原子性：事务要么全部完成，要么全部不完成\n- 一致性：在事务操作前和操作后，数据满足完整性约束\n- 隔离性：并发执行的事务不会互相干扰\n- 持久性：事务一旦执行完毕，对数据的修改就是永久的\n### 隔离性有哪些隔离级别\n- 读未提交：事务未提交时就能看到更改\n- 读提交：事务提交后能看到更改\n- 可重复读：事务执行过程中看到的数据，和事务启动时看到的数据是一样的。这是**MySQL InnoDB 引擎的默认隔离级别**\n- 串行化：对数据加上读写锁，必须按照顺序读写\n可能会发生的问题\n- 读未提交：脏读、幻读、不可重复读\n- 读提交：不可重复读、幻读\n- 可重复读：幻读\n- 串行化：无\nMysql默认使用可重复读，可以很大程度减少幻读现象的发生。虽然使用串行化可以确保不发生，但是性能开销太大。\n### 什么是间隙锁\n间隙锁存在于可重复读隔离级别，用来解决数据库幻读的问题\n- 如果表中存在一个(3, 5)的间隙锁，那么其他事务就无法插入id=4的记录了，事务会阻塞住，直到这条事务结束。\n### Mysql如何保证原子性\n使用undo log日志，在开始执行事务之前记录undo log\n### Undo log具体怎么撤销的\n当引擎对一条数据进行操作（修改、删除、新增）时，就会把要回滚的数据记录在undo log中。\n- 修改一条数据：记录主键值和修改前的旧值\n- 删除：记录主键值和旧值\n- 新增：记录主键值\n### 什么时候需要建立索引\n- 字段名唯一\n- 经常用where查询的字段\n- 经常用group by和order by的字段，因为B+树中的记录都是有序的\n### 什么时候不需要建立索引\n- where、group by和order by不经常用到的字段\n- 字段中包含大量重复数据，例如性别。innodb引擎有一个查询优化器，当它发现某个值在表中占比很高时，它会自动查询整个表，忽略索引\n- 表数据太少\n- 字段值经常发生改变\n## Java\n---\n### 线程有哪些状态\n- NEW：线程创建\n- RUNNABLE：就绪火正在运行\n- BLOCKED：阻塞\n- WAITING：等待另一线程执行操作\n- TIMED_WAITING：具有指定等待时间的等待\n- TERMINATED：终止\n### waiting中的线程如何变成running\n- 等待的线程被其他线程唤醒\n-",
  "Spring 面试速查表": "# Spring\n---\n## 说一下你对Spring的理解\nSpring框架有四个特点：\n- IoC框架：通过控制反转实现对象的创建和依赖管理。开发者只要定义好bean和依赖关系，剩下交给spring自动注入。\n- 面向切片编程：允许开发者横切关注点。\n- 事务管理：Spring提供了一致的事务管理接口。\n- MVC框架。\n## Spring的核心思想\n- IoC控制反转\n- DI依赖注入\n- AOP面向切片编程\n## Spring Ioc\n即控制反转。一般我们使用new来获得对象，但是IoC思想使用IoC容器帮助我们实例化对象。可以大大降低对象间的耦合度。\n## Spring AOP\n面向切面编程。能够将那些与业务无关，却又为业务所共同调用的逻辑封装起来，降低系统的耦合度。\n在AOP中，一个程序最小的单元是切面。一个切面包括很多类型和对象，对他们进行统一管理。\n在AOP中，功能分为核心功能和周边功能，这两项功能独立进行开发，互补耦合。在最后将切面功能和核心功能编制在一起，这就是AOP。\n## Spring IoC是怎么实现的\n- 反射：Spring IoC使用Java反射机制动态地加载类与方法。\n- 依赖注入：容器负责管理程序组件之间的依赖关系。\n- 工厂模式：容器作为工厂负责实例化Bean并管理它们的生命周期\n- 容器实现：使用BeanFactory来管理Bean\n## Spring AOP是怎么实现的\n基于动态代理技术。\n- 基于JDK的动态代理\n- 基于CGLIB的动态代理\n## 怎么理解SpringIoC\nIoC即控制反转，是一种设计思想。传统的Java程序中，在对象内部使用new创建对象，属于程序主动创建依赖对象。\n在Spring中，有专门的容器控制对象。\n所谓控制，就是对象的创建、初始化、销毁。所谓反转，是反转的控制权，对象的控制完全脱离了我们，而是将控制权交给了Spring。\n## 依赖倒置、依赖注入\n- 依赖倒置：高层模块不依赖底层模块，它们共同依赖同一个抽象。抽象不依赖实现细节，实现细节依赖抽象。\n- 依赖注入：不通过new新建对象，而是将依赖的类的对象在外部创建好之后，注入给类来使用。\n## 怎么实现依赖注入\n- 构造器注入（最推荐）\n\t```java\n@Autowired\n    public UserServiceImpl(UserMapper userMapper, StringRedisTemplate stringRedisTemplate, JavaMailSender mailSender) {\n        this.userMapper = userMapper;\n        this.stringRedisTemplate = stringRedisTemplate;\n        this.mailSender = mailSender;\n    }\n\t```\n- Setter方法注入（会导致依赖未完全初始化）\n\t```java\n@Autowired\n    public void setOcMapper(OcMapper ocMapper) {\n        this.ocMapper = ocMapper;\n    }\n\t```\n- 字段注入（会隐藏实际依赖关系，违反依赖导致原则，不推荐）\n\t```java\n@Autowired\n    private OcMapper ocMapper;\n\t```",
  "Java基础 面试速查表": "# 概念\n---\n## Java的特点\n1. 面向对象\n2. 平台无关性\n3. 内存管理\n## Java的优势和劣势是什么？\n- 优势：\n\t跨平台、面向对象、优秀的社区生态、自动垃圾回收机制、安全性。\n- 劣势：\n\t速度比原生语言慢、JVM对资源有限的环境不友好、面向对象过于严格。\n## Java为什么可以跨平台\n因为由Java运行依托的JVM有不同平台的版本，Java编译生成的字节码文件可以直接在JVM上运行。\n## JVM、JRE、JDK区别和关系\n- JVM：Java虚拟机，负责将Java字节码翻译成机器码并运行程序。提供内存管理、垃圾回收等机制，使得Java可以跨平台。\n- JRE：Java运行时环境，包括一组类库和JVM。\n- JDK：包含了Java运行、编译的所有环境。包括类库，JVM、Java编译器（javac）和Java调试器（jbd）等等。\n## Java为什么解释和编译都有？\nJava经过编译后生成字节码文件，再由JVM解释字节码文件运行。\n# 数据类型\n---\n## 八种基本数据类型\n- 数值型：整数：byte(8)、short(16)、int(32)、long(64) 浮点型：float(32)、double(64)\n- 字符型：char(2)\n- 布尔型：boolean(无明确上限)\n引用数据类型：class、interface和数组\n## 数据类型的转换方式\n- 隐式类型转换：当目标类型大于原类型时，会自动转换，如int转换为long\n- 显式类型转换：int = (long) long\n- 字符串转换：可以将字符串转换为其他数据类型。可以使用包装类下的方法，如`Integer.parseInt()`\n- 数值之间的转换：可以使用包装类中的方法将字符型转换为整型。\n## 类型转换会出现什么问题\n- 数据丢失\n- 数据溢出\n- 精度丢失\n- 类型不匹配\n## 为什么用BigDecimal不用double\ndouble会出现精度丢失的问题，在牵扯金钱计算的时候，都使用BigDecimal\nBigDecimal使用字符串创建，不能使用double创建。\n## 装箱和拆箱是什么\n基本数据类型和对应的包装类之间进行转化的过程。\n- 自动装箱发生的时间：赋值、方法调用\n- 自动装箱的弊端：在循环中进行自动装箱会创建多余的对象，影响程序的性能\n## 为什么Java要有Integer\n- 能够将数据和处理这些数据的方法结合在一起。例如Integer可以用parseint来处理字符串转换。\n- Java中的绝大部分方法都只能处理类型，例如不能给List传入int参数。\n- Java中基本类型和引用类型不能直接转换，必须使用包装类包装才能转换。\n- 集合中只能储存包装类。\n## Integer缓存\n在Integer类内部有一个缓存池，用来存储-128到127的数据。当用到这些数据时，会直接从内存中取出，不需要新建对象。\n# 面向对象\n---\n## 怎么理解面向对象\n面向对象是将现实生活中的事物抽象为对象。对象拥有属性（字段）和行为（方法）\nJava面向对象的三大特性：封装、继承、多态\n- 封装：只能通过对象提供的方法与外界交互。\n- 继承：继承可以使子类复用父类方法。是代码重用的重要手段。\n- 多态：可以使不同类的对象对同一操作做出反应，包括重载和重写。\n## 多态体现在哪里\n- 方法重写\n- 方法重载\n- 接口与实现\n- 向上转型和向下转型\n## 面向对象的六大设计原则\n- 单一职责原则\n- 开放封闭原则\n- 里氏替换原则\n- 接口隔离原则\n- 依赖倒置原则\n- 最少知识原则\n## 重载和重写的区别\n- 重载：在一个类中，可以有多个同名的方法，它们的参数列表不同。\n- 重写：在子类中可以重写父类的方法。但是方法名、参数列表和返回类型必须和父类一致。\n## 抽象类和普通类的区别\n- 抽象类不能被实例化。\n- 普通类必须有方法实现，抽象类可以没有。\n## 抽象类和接口的区别\n- 抽象类可以有成员变量，构造方法和具体方法。\n- 接口只能有常量和抽象方法。\n区别：\n- 一个类只能继承一个抽象类，但是可以继承多个接口\n- 接口只能有方法定义，不能有实现。抽象类可以有默认方法。\n- 接口成员变量默认为public static final，必须有初值，不能被修改。成员方法为public、abstarct的。\n\t抽象类中的成员变量可以被重新定义。\n## 接口中可以定义哪些方法\n抽象（abstract）、默认、静态（static）、私有（private）方法。、\n私有方法只能在接口内部访问，不能被实现类访问。\n## Java中的静态变量和静态方法\n静态意味着它是和类本身关联的，而不是和类的对象关联的。它们在内存中只存在一份， 可以通过类本身调用，被类的所有实例共享。\n静态方法不能访问非静态的成员变量和方法。但可以直接调用其他静态变量和静态方法。静态方法不能被重写，但可以被隐藏。\n# 关键字\n---\n## Java中final关键字的作用是什么\n- 修饰类时：这个类不能被继承\n- 修饰变量时：常量，不能修改\n- 修饰方法：不能被子类重写\n# 深拷贝与浅拷贝\n---\n## 深拷贝和浅拷贝的区别\n- 浅拷贝只赋值对象中的值类型字段，对于引用字段，不会创建一个新的对象。\n- 深拷贝既赋值值类型字段，也会重新创建一个新的对象。\n## 深拷贝的三种实现方法\n- 实现Cloneable()接口并重写clone()方法\n\t```java\npublic class ClassB implements Cloneable{\n    private int b;\n    private ClassA cla;\n\n    @Override\n    protected Object clone() throws CloneNotSupportedException {\n        ClassB clonedClass = (ClassB) super.clone();\n        clonedClass.cla = (ClassA) cla.clone();\n        return clonedClass;\n    }\n}\n\npublic class ClassA implements Cloneable{\n    private int a;\n\n    @Override\n    protected Object clone() throws CloneNotSupportedException {\n        return super.clone();\n    }\n}\n\t```\n- 使用序列化和反序列化\n- 手动递归复制\n# 对象\n---\n## Java创建对象有几种方式\n- 通过new创建\n- 通过反射机制创建\n\t可以通过反射获取类名，然后调用newInstance()获取\n- 通过clone()方法获取\n- 通过反序列化获取\n## New出的对象什么时候回收\nJava垃圾回收器会周期性地检测不再被引用的对象。\n- 使用计数器检测对象是否被引用。\n- 使用可达性分析算法，分析一个对象是否可达。\n## 如何获得私有对象\n- 如果类遵循了良好的代码规范的话，可以使用getter方法获取对象。\n- 可以通过反射方法绕过private获取对象。\n# 反射\n---\n反射机制是指在运行过程中，可以获得并调用一个类的所有字段和方法，同时可以获得他的类名、包名、父类等等。\n- 运行时类信息访问：可以在运行时获得类的类名、包名、父类等等\n- 动态对象创建：即使不知道具体类名，也可以通过Class的newInstance创建对象\n- 动态方法调用：可以调用对象的私有方法。\n- 访问和修改字段值：可以访问并修改字段值，即使是私有的。\n# 异常\n---\n## Java异常的分类\nJava异常主要有两部分：Error和Exception\n- 错误Error：这是指运行环境的错误，程序不应该捕获这类错误。错误是无法处理的严重错误。\n- 异常Exception：表示程序本身可以处理的错误。\n\t- 非运行时异常：在编译期就应该捕获并抛出的异常，比如类不存在\n\t- 运行时异常：在运行期捕获的异常，如数组越界\n## Java的异常处理\n- 使用try-catch块处理异常。\n```java\ntry {\n        //可能抛出异常的代码\n        } catch(ExceptionType1 e) {\n            //异常类型1的处理\n        } catch(ExceptionType2 e) {\n            //异常类型2的处理\n        } catch(ExceptionType3 e) {\n            //异常类型3的处理\n        } finally {\n            //可选的finally块，用于定义无论是否发生异常都会执行的代码\n}\n```\n- 使用throw语句手动抛出异常。\n```java\nthrow new Exception();\n```\n- 使用throws关键字：如果不想在方法内部处理异常，可以使用throws关键字将异常传递给调用者处理。\n```java\npublic void method() throws Exception {\n}\n```\n- finally块：无论是否发生异常都会执行的代码块。\n# Object\n---\n## ==和equal有什么区别\n- 对于字符串对象，==对比的是两个变量在内存中的首地址，属于数值比较。对于非字符串对象，两者效果是相同的。\n- equals比较的内容，比较两个字符串的内容是否相等。\n## hashCode和equal方法有什么关系\n- 一致性：如果`a.equal(b)`，则两个对象的hashcode一定相等。\n- 非一致性：如果两个对象的hashcode相等，`a.equal(b)`不一定为true。\n## String、StringBuffer、StringBuilder\n- String线程安全，因为每次修改都会创建一个新的对象。但是开销最大。\n- StringBuilder非线程安全。可以直接修改，不会创建新对象，适合单线程使用。性能开销最小。\n- StringBuffer线程安全，可以直接修改。性能开销比StringBuilder大，因为增加了线程安全机制。\n# Java8新特性\n---\n## Lambda表达式\nLambda用于创建匿名函数，有两种表示方法。\n- `(parameters) -> expression `表达式的值作为返回值\n- `(parameters) → { statements; } `需要在statments中手动return\nLambda可以使代码更加清晰明了，但是过多使用lambda会增加调试困难。\n## Stream API\n流API提供了一种高效处理集合对象的方式，包括过滤、映射、排序等。\n## Stream流的并行API是什么\n是ParallelStream。并行流将元数据拆分成数个小任务多线程进行流操作，操作结束之后将结果合并。底层是使用通用的fork/join池来进行的。",
  "Docker": "## 关于Docker\nDocker是一个开源的应用容器引擎，让开发者可以打包他们的应用以及依赖包到一个可移植的容器中，然后发布到任何流行的Linux机器上，也可以实现虚拟化，容器是完全使用沙箱机制，相互之间不会有任何接口。\n## Docker服务相关命令\n```shell\n#启动docker服务\nsystemctl start docker\n\n#查看docker服务\nsystemctl status docker\n\n#关闭docker服务\nsystemctl stop docker\n\n#重启docker服务\nsystemctl restart docker\n\n#开机启动docker\nsystemctl enable docker\n```\n## Docker镜像相关命令\n```shell\n#查看本地镜像列表\ndocker images\n\n#搜索远程镜像\ndocker search xxx\n```\n搜索远程镜像源使用阿里云的镜像加速器会有问题，它还是会从docker自己的服务器走，不会走加速器，~~但是拉取镜像正常。~~\n实际上还是有问题。使用的方法是把下面一大串的地址全部塞进守护进程配置文件中，反正肯定有一个地址能用：\n```json\n{\n  \"registry-mirrors\": [\n    \"https://2a6bf1988cb6428c877f723ec7530dbc.mirror.swr.myhuaweicloud.com\",\n    \"https://docker.m.daocloud.io\",\n    \"https://hub-mirror.c.163.com\",\n    \"https://mirror.baidubce.com\",\n    \"https://your_preferred_mirror\",\n    \"https://dockerhub.icu\",\n    \"https://docker.registry.cyou\",\n    \"https://docker-cf.registry.cyou\",\n    \"https://dockercf.jsdelivr.fyi\",\n    \"https://docker.jsdelivr.fyi\",\n    \"https://dockertest.jsdelivr.fyi\",\n    \"https://mirror.aliyuncs.com\",\n    \"https://dockerproxy.com\",\n    \"https://mirror.baidubce.com\",\n    \"https://docker.m.daocloud.io\",\n    \"https://docker.nju.edu.cn\",\n    \"https://docker.mirrors.sjtug.sjtu.edu.cn\",\n    \"https://docker.mirrors.ustc.edu.cn\",\n    \"https://mirror.iscas.ac.cn\",\n    \"https://docker.rainbond.cc\"\n  ]\n}\n```\n可以访问hub.docker.com手动找。\n```shell\n#拉取远程镜像\ndocker pull xxx:version\n\n#删除本地镜像\ndocker rmi id\n\n#删除所有镜像\ndocker rmi 'dooker images -p'\n```\nid是一个镜像的唯一标识符，会有多个镜像依赖这个标识符。只有把相关的镜像全部删除才会真正删除文件。\n## Docker容器相关命令\n```shell\n# 从镜像创建容器\ndocker run -it --name=c1 centos:7\n# -i 使得可以与容器交互\n# -t 创建一个伪终端，使得可以与容器交互\n# -d 以分离模式运行容器，使得容器可以在后台运行。需要使用exec命令进入容器\n\n# 查看容器列表\ndocker ps -aq\n# -a 查看历史所有的容器（包括已经关闭的）\n# -q 查看容器id\n\n# 进入已经打开的容器\ndocker exec -it c3 /bin/bash\n```\n使用-it创建的容器称为交互式容器 ，exit后会自动退出；使用-id创建的容器称为守护式容器，退出之后容器不会关闭\n```shell\n# 停止容器\ndocker stop c1\n\n# 删除容器\ndocker rm c1\n# 不能删除正在运行的容器\n# 结合ps命令可以这样用，删除所有容器\ndocker rm `docker ps -aq`\n\n# 查看详细信息\ndocker inspect c1\n```\n## 数据卷\n容器是一个黑箱，它不能直接与其他容器或者外部机器通信或者传输文件，但它可以与宿主机之间共享文件。因此，在宿主机指定一个目录，将这个目录挂载到容器后，这个目录下的所有修改即可在容器与外界之间同步。这个目录称为数据卷。\n```shell\n# 在指定目录下创建数据卷\ndocker run -id --name=c1 -v ~/data:/root/data\n```\n-v命令用于创建数据卷。指定的路径必须是绝对路径，如果路径不存在会自动创建。\n## 数据卷容器\n如果将每个容器都手动挂载到一个路径下，当容器数量很多时，这一操作会变得非常繁琐，而且不利于后续维护。Docker提供了一个专门的容器——数据卷容器用于管理数据卷。当两个容器同时挂载这个容器时，相当于挂载了同一个目录。\n```shell\n# 创建并启动数据卷容器，并设置数据卷\ndocker run -it --name=c3 -v /volume centos:7\n\n# 创建容器，并使用--volumes-from参数设置数据卷\ndocker run it --name=c1 --volumes-from c3 centos:7\ndocker run it --name=c2 --volumes-from c3 centos:7\n```\n\n##  部署MySQL应用\n搜索mysql镜像\n```shell\ndocker search mysql\n```\n拉取mysql镜像\n```shell\ndocker pull mysql:5.6\n```\n创建容器，设置端口映射、目录映射\n```shell\n# 在/root目录下创建mysql目录用于存储mysql数据信息\nmkdir ~/mysql\ncd ~/mysql\n```\n```shell\ndocker run -id \\\n-p 3307:3306 \\\n--name=c_mysql \\\n-v $PWD/conf:/etc/mysql/conf.d \\\n-v $PWD/logs:/logs \\\n-v $PWD/data:/var/lib/mysql \\\n-e MYSQL_ROOT_PASSWORD=123456 \\\nmysql:5.6\n```\n- 参数说明：\n\t- **p 3307:3306**：将容器的 3306 端口映射到宿主机的 3307 端口。\n\t- **v \\$PWD/conf:/etc/mysql/conf.d**：将主机当前目录下的 conf/my.cnf 挂载到容器的 /etc/mysql/my.cnf。配置目录\n\t- **v \\$PWD/logs:/logs**：将主机当前目录下的 logs 目录挂载到容器的 /logs。日志目录\n\t- **v \\$PWD/data:/var/lib/mysql** ：将主机当前目录下的data目录挂载到容器的 /var/lib/mysql 。数据目录\n\t- **e MYSQL_ROOT_PASSWORD=123456：**初始化 root 用户的密码。\n进入容器，操作mysql\n```shell\ndocker exec –it c_mysql /bin/bash\n```\n##  部署Tomcat应用\n搜索tomcat镜像\n```shell\ndocker search tomcat\n```\n拉取tomcat镜像\n```bash\ndocker pull tomcat\n```\n创建容器，设置端口映射、目录映射\n```shell\n# 在/root目录下创建tomcat目录用于存储tomcat数据信息\nmkdir ~/tomcat\ncd ~/tomcat\n```\n```shell\ndocker run -id --name=c_tomcat \\\n-p 8080:8080 \\\n-v $PWD:/usr/local/tomcat/webapps \\\ntomcat\n```\n- 参数说明：\n\t- **p 8080:8080：**将容器的8080端口映射到主机的8080端口\n\t\t- **v \\$PWD:/usr/local/tomcat/webapps：**将主机中当前目录挂载到容器的webapps\n## 部署Nginx应用\n搜索nginx镜像\n```shell\ndocker search nginx\n```\n拉取nginx镜像\n```shell\ndocker pull nginx\n```\n创建容器，设置端口映射、目录映射\n```shell\n# 在/root目录下创建nginx目录用于存储nginx数据信息\nmkdir ~/nginx\ncd ~/nginx\nmkdir conf\ncd conf\n# 在~/nginx/conf/下创建nginx.conf文件,粘贴下面内容\nvim nginx.conf\n```\n```shell\n\nuser  nginx;\nworker_processes  1;\n\nerror_log  /var/log/nginx/error.log warn;\npid        /var/run/nginx.pid;\n\n\nevents {\n    worker_connections  1024;\n}\n\n\nhttp {\n    include       /etc/nginx/mime.types;\n    default_type  application/octet-stream;\n\n    log_format  main  '$remote_addr - $remote_user [$time_local] \"$request\" '\n                      '$status $body_bytes_sent \"$http_referer\" '\n                      '\"$http_user_agent\" \"$http_x_forwarded_for\"';\n\n    access_log  /var/log/nginx/access.log  main;\n\n    sendfile        on;\n    #tcp_nopush     on;\n\n    keepalive_timeout  65;\n\n    #gzip  on;\n\n    include /etc/nginx/conf.d/*.conf;\n}\n\n\n```\n在创建容器时一定要退出到根目录。\n```shell\ndocker run -id --name=c_nginx \\\n-p 80:80 \\\n-v $PWD/conf/nginx.conf:/etc/nginx/nginx.conf \\\n-v $PWD/logs:/var/log/nginx \\\n-v $PWD/html:/usr/share/nginx/html \\\nnginx\n```\n- 参数说明：\n\t- **p 80:80**：将容器的 80端口映射到宿主机的 80 端口。\n\t- **v \\$PWD/conf/nginx.conf:/etc/nginx/nginx.conf**：将主机当前目录下的 /conf/nginx.conf 挂载到容器的 :/etc/nginx/nginx.conf。配置目录\n\t- **v \\$PWD/logs:/var/log/nginx**：将主机当前目录下的 logs 目录挂载到容器的/var/log/nginx。日志目录\n## DockerFile\nDockerfile 是一个文本文件，包含一系列指令，用于定义如何构建 Docker 镜像。每个指令对应一个镜像层，Docker 通过执行这些指令来自动化镜像的构建过程。",
  "Mybatis": "## **入门**\n### **入门程序**\n目的：使用Mybatis查询表中所有分类信息。\n- 第一步：构建一个mybaits项目，一个Category实体类（用以储存查询到的信息），数据库表category\n在构建Category实体类时，需要有一个全参构造器和所有get()set()函数，字段列表类型，名称需要与数据库的字段列表一致。其中tinyint使用Short代替，varchar使用String代替。\n```plain text\npublic class Category {\n    private Integer id;\n    private String name;\n    private Short type;\n    private Short sort;\n    private Short status;\n\n    public Category(Integer id, String name, Short type, Short sort, Short status) {\n        this.id = id;\n        this.name = name;\n        this.type = type;\n        this.sort = sort;\n        this.status = status;\n    }\n\n    get()与set()函数\n    toString()函数\n}\n```\n- 引入mybatis相关依赖，并配置mybatis\n打开application.properties文件，并在其中填入如下的语句：\n```plain text\n# 配置数据库的链接信息\n# 驱动\nspring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver\n# url\nspring.datasource.url=jdbc:mysql://localhost:3306/mybatis\n# 用户名\nspring.datasource.username=root\n# 密码\nspring.datasource.password=1234\n```\n- 使用注解（或xml）编写sql语句\n声明一个CategoryMapper接口，在其前面加上@Mapper注解。这个注解会自动将其封装成实体类（可以使用其类对象）。\n```plain text\n@Mapper\npublic interface CategoryMapper {\n\n    //Select注解表示需要执行的sql语句\n    @Select(\"select * from category\")\n    public List<Category> list();  //使用List来接受多行数据\n}\n```\n在test文件中进行测试。使用@Autowired来自动将相关依赖注入接口类对象。\n```plain text\n @Autowired\n    private CategoryMapper categoryMapper;\n\n @Test\n    void contextLoads() {\n        List<Category> categoryList = categoryMapper.list();\n        categoryList.stream().forEach(category -> {\n            System.out.println(category);\n        });\n    }\n```\n### **配置SQL提示**\n@Select注解中的字符串并没有自动联想与自动纠错。选择@Select中的字符串，找到Show Context Actions-\\>Inject language reference-\\>MySql即可\n### **JDBC**\nJDBC是sun公司提供的一套操作所有关系型数据库的规范，即接口。各个数据库厂商自己实现接口，提供数据库驱动jar包。使用JDBC编程时，执行的代码是jar包中的实现类。\n### **Lombok**\nLombok是一个实用的java类库，能通过注解的形式自动生成构造器等函数。\n<table>\n<tr>\n<td>**注解**</td>\n<td>**作用**</td>\n</tr>\n<tr>\n<td>@Getter/@Setter</td>\n<td>添加get/set方法</td>\n</tr>\n<tr>\n<td>@ToString</td>\n<td>生成易阅读的tostring方法</td>\n</tr>\n<tr>\n<td>@EqualsAndHashCode</td>\n<td>重写equals方法和hashCode方法</td>\n</tr>\n<tr>\n<td>@Data</td>\n<td>提供了更综合的生成代码功能（涵盖以上）</td>\n</tr>\n<tr>\n<td>@NoArgsConstructor</td>\n<td>无参构造</td>\n</tr>\n<tr>\n<td>@AllArgsConstructor</td>\n<td>全参构造</td>\n</tr>\n</table>\n引入依赖：\n```plain text\n<dependency>\n    <groupId>org.projectlombok</groupId>\n    <artifactId>lombok</artifactId>\n </dependency>\n```\n## **基础操作**\n### **删除**\n使用@Delete来指定SQL语句与函数。\n函数的返回值可以用来记录操作影响的数据数量，使用int接受。#\\{\\}是一个占位符，用来捕获传递给函数的参数，并在语句中使用其参数。<br>如果接口方法形参只有一个普通类型的参数，#\\{\\}里的属性名可以随便写。\n```plain text\n@Mapper\npublic interface MenuMapper {\n\n    //根据ID删除数据\n    @Delete(\"delete from mybatis.drinks where id = #{id}\")    //#{}是一个占位符\n    public int delete(Integer deleteId);                    //返回值代表操作的记录数\n}\n```\n### **预编译SQL**\n默认情况下，mybatis日志不会输出，使用\n```plain text\n# 打开mybatis的日志，并输出到控制台\nmybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl\n```\n来将日志输出到控制台。\n- 预编译SQL\n当使用#\\{\\}来指定参数时，会出现预编译SQL，#\\{\\}被解释为?，例如：\n```plain text\n==>  Preparing: delete from mybatis.drinks where id = ?\n==> Parameters: 22(Integer)\n```\n使用预编译SQL性能更高，且更加安全（防止SQL注入）。\n- SQL注入\nSQL注入是通过操作输入的数据来修改事先定义好的SQL语句，达到执行代码对服务器进行攻击的操作。\n例如，登录的SQL写法：\n```plain text\nselect count(*) from emp where username = 'username' and password = '1234'\n```\n而当输入的密码为'or'1' = '1时，SQL语句将变为：\n```plain text\nselect from(*) from emp where username = 'username' and password = ''or'1' = '1'\n```\n判断语句永远为真。这样就达到了攻击服务器的目的。\n而使用预编译SQL即可预防这种情况。当然也可以使用\\$\\{\\}使用拼接SQL语句，使用场景很少。\n### **新增**\n使用@Insert注解，具体实现如下：\n```plain text\n@Insert(\"insert into mybatis.drinks(id, name, category_id, price, img, description, status, update_time)\" +\n            \" values (#{id}, #{name}, #{categoryId}, #{price}, #{img}, #{description}, #{status}, #{updateTime}) \")\n    public void insert(Drinks drinks);\n```\n可以将传递的参数封装在一个对象中，且作为参数传递。注意：#\\{\\}中的参数需要与对象中一致，即遵守驼峰命名法。\n### **主键返回**\n将封装的实体类传递给insert函数后，实体类会被清空，因此无法拿到当初填入的主键。可以使用@Options来进行一些设置，其中，keyProperty表示返回的主键 useGeneratedKeys = true表示将返回的主键值封装在字段中。\n```plain text\n@Options(keyProperty = \"id\", useGeneratedKeys = true)\n//使用@Options注解配置主键信息，useGeneratedKeys = true表示将返回的主键值封装在字段中\n@Insert(\"insert into mybatis.drinks(id, name, category_id, price, img, description, status, update_time)\" +\n        \" values (#{id}, #{name}, #{categoryId}, #{price}, #{img}, #{description}, #{status}, #{updateTime}) \")\npublic void insert2(Drinks drinks);\n```\n### **更新**\n```plain text\n@Update(\"update mybatis.user set id = #{id}, username = #{username}, password = #{password}\")\npublic void update(Users users);\n```\n### **查询**\n```plain text\n@Select(\"select * from mybatis.drinks where id = #{id}\")\npublic Drinks getById(Integer id);\n```\n查询方法返回值可以是一个集合（用来储存多个返回内容），可以是一个对象（返回单个元素），int（使用了count()函数）。\n如果实体属性名与数据库表查询返回的字段名一致，mybatis会自动封装；如果不一致，则不会自动封装。解决方法如下：\n- 方案一：起别名\n```plain text\n@Select(\"select id, name, category_id categoryId, price, img, description, status, update_time updateTime from mybatis.drinks where id = #{id}\")\npublic Drinks getById(Integer id);\n```\n- 方案二：使用@Results和@Result注解自动映射\n```plain text\n@Results({\n            @Result(column = \"category_id\", property = \"categoryId\"),\n            @Result(column = \"update_time\", property = \"updateTime\"),\n})\n@Select(\"select * from mybatis.drinks where id = #{id}\")\npublic Drinks getById(Integer id);\n```\n- 方案三：使用mybatis驼峰命名自动映射\n在application.properties中配置`mybatis.configuration.map-underscore-to-camel-case=true`即可。使用这种方法需要严格遵守：数据库中字段使用下划线命名，mybatis中使用驼峰命名法。\n### **条件查询**\n目的：对name进行模糊查询，查询指定价格范围内的内容并以倒序排序：\n```plain text\n@Select(\"select * from mybatis.drinks where name like concat('%', #{name}, '%') and status = #{status} and price between #{price1} and #{price2} order by category_id desc\")\npublic List<Drinks> getBy(String name, Integer status, Double price1, Double price2);\n```\n需要注意的是，name后不是等于，是like；而且不能使用预编译SQL，需要使用字符串拼接函数concat()",
  "MySQL": "## **SQL简介**\nSQL是一门操作型数据库的编程语言，定义操作所有关系型数据库的统一标准。\n- SQL语句可以单行或多行书写，以分号结尾。\n- SQL语句可以使用空格或缩进来增加可读性。\n- MySQL数据库的SQL语句不区分大小写。\n- 注释：-- 注释内容或/\\*\\*/\nSQL分为四类：\n<table>\n<tr>\n<td>**分类**</td>\n<td>**全称**</td>\n<td>**说明**</td>\n</tr>\n<tr>\n<td>DDL</td>\n<td>Data Definition Language</td>\n<td>数据定义语言，用来定义数据库对象</td>\n</tr>\n<tr>\n<td>DML</td>\n<td>Data Manipulation Language</td>\n<td>数据操作语言，用来对数据库表中的数据进行增删改</td>\n</tr>\n<tr>\n<td>DQL</td>\n<td>Data Query Language</td>\n<td>数据查询语言，用来查询数据库表中的记录</td>\n</tr>\n<tr>\n<td>DCL</td>\n<td>Data Control Language</td>\n<td>数据控制语言，用来创建数据库用户，控制数据库的访问权限</td>\n</tr>\n</table>\n## **SQL语法**\n### **DDL：Data Definition Language**\n### 增\n- 创建数据库：\n```plain text\ncreate database name;\n```\n- 删除数据库：\n```plain text\ndrop database name;\n```\n- 创建表<br>基本形式：\n```plain text\ncreate table tableName {\n    字段 字段类型 [约束] [commont 字段注释]\n} comment 表注释\n```\n实例：\n```plain text\ncreate table tb_user(\n    id int primary key comment 'ID，唯一标识',\n    username varchar(20) comment '用户名',\n    name varchar(10) comment '姓名',\n    age int comment '年龄',\n    gender char(1) comment '性别'\n) comment '用户信息';\n```\n约束共有五种：\n<table>\n<tr>\n<td>**约束**</td>\n<td>**描述**</td>\n<td>**关键字**</td>\n</tr>\n<tr>\n<td>非空约束</td>\n<td>限制该字段值不能为null</td>\n<td>not null</td>\n</tr>\n<tr>\n<td>唯一约束</td>\n<td>保证字段的所有数据都是唯一，不重复的</td>\n<td>unique</td>\n</tr>\n<tr>\n<td>主键约束</td>\n<td>主键是一行的唯一标识，要求非空且唯一</td>\n<td>primary key</td>\n</tr>\n<tr>\n<td>默认约束</td>\n<td>保存数据时，如果未指定该字段值，则采用默认值</td>\n<td>default</td>\n</tr>\n<tr>\n<td>外键约束</td>\n<td>让两张表的数据建立连接，保证数据的一致和完整性</td>\n<td>foreign key</td>\n</tr>\n</table>\n可以使用auto_increment让主键自动增长。\n### 查\n- 查表：`showtables` 查看当前数据库下的表\n- 查看表元素：`desc table_name` 查看表中的元素信息\n- 查看建表语句：`shwo create table table_name`\n### 改\n- 添加字段：`alter table table_name add colunm_name type(lenth) comment constraint`\n- 修改字段类型：`alter table table_name modify colunm_name new_type(lenth)`\n- 修改字段名和字段类型：`alter table table_name change old_colunm_name new_colunm_name new_type(lenth) comment constraint`\n- 删除字段：`alter table table_name drop colunm colunm_name`\n- 修改表名：`rename table table_name to new_table_name`\n### **DML: Data Manipulation Language**\nDML语言时一种对数据库的数据记录进行增删改的操作。\n### Insert插入\n- 插入指定字段的值：\nnow()函数可以返回当前系统时间。\n```plain text\ninsert into tb_emp(username, name, gender, create_time, update_time) values('KringKoter', '克林沃特', 1, now(), now());\n```\n- 插入全部字段的值：\n```plain text\ninsert into tb_emp\n            values('AfternoonTea2', null, '阿伏特罗', 123, 1, null, 1, null, now(), now());\n```\n- 批量插入数据：\n```plain text\ninsert into tb_emp(username, name, create_time, update_time)\n            values('Kring', '克林', now(), now()), ('Koter', '沃特', now(), now());\n```\n### Update更新\n- 更新单个字段的值：\n```plain text\nupdate tb_emp set name = '牛魔', update_time = now() where id = 1;\n```\nwhere后是更新条件，只有满足条件才会更新。\n- 更新所有字段的值：\n不加where，通常会出警告。\n### Delete删除\n- 删除单条数据：\n```plain text\ndelete from tb_emp where id = 1\n```\n- 删除全部数据：\n```plain text\ndelete from tb_emp;\n```\n*delete不能删除单条字段的值，若需要此操作，可使用update将该字段设置为null。*\n### **DQL：Data Query Language**\n关键字select\n### 基本查询\n```plain text\n#查找多个字段\nselect name, username from tb_emp;\n\n#查找全部字段（不推荐\nselect * from tb_emp\n\n#设置别名\nselect name as '姓名', username as '用户名' from tb_emp;\n\n#去除重复记录\nselect distinct gender from tb_emp;\n```\n### 条件查询\n```plain text\nselect colunm_list from table_name where condition_list\n```\n基本条件列表如下：\n<table>\n<tr>\n<td>**运算符**</td>\n<td>**功能**</td>\n</tr>\n<tr>\n<td>\\>, \\>=, \\<, \\<=, =</td>\n<td>大于，大于等于，小于，小于等于，等于</td>\n</tr>\n<tr>\n<td>!=</td>\n<td>不等于</td>\n</tr>\n<tr>\n<td>between ... and ...</td>\n<td>在某个范围内，闭区间</td>\n</tr>\n<tr>\n<td>in(...)</td>\n<td>在括号内的值，多选一</td>\n</tr>\n<tr>\n<td>like _/%</td>\n<td>模糊匹配，_匹配单个字符，%匹配任意字符</td>\n</tr>\n<tr>\n<td>is null</td>\n<td>是否为空</td>\n</tr>\n<tr>\n<td>&&， \\|\\|</td>\n<td>并/且</td>\n</tr>\n<tr>\n<td>!</td>\n<td>非</td>\n</tr>\n</table>\n### 聚合函数\n聚合函数是将一列数据作为一个整体，进行纵向计算的函数。语法：`select 聚合函数(字段列表) from 表名`;\n以下是聚合函数的类型：\n<table>\n<tr>\n<td>**聚合函数**</td>\n<td>**功能**</td>\n</tr>\n<tr>\n<td>count</td>\n<td>计数</td>\n</tr>\n<tr>\n<td>max</td>\n<td>求最大值</td>\n</tr>\n<tr>\n<td>min</td>\n<td>求最小值</td>\n</tr>\n<tr>\n<td>avg</td>\n<td>求平均值</td>\n</tr>\n<tr>\n<td>sum</td>\n<td>求和</td>\n</tr>\n</table>\n### 分组查询\n语法：\n```plain text\nselect 字段列表 from 表名 [where 外筛选条件] group by 分组字段名 [having 分组后筛选条件]\n```\n字段列表无法单独为表中的字段，需要搭配聚合函数使用，无法是单独的\\*\n### 排序查询\n语法：\n```plain text\nselect 字段列表 from 表名 [where 条件] group by 分组字段名 order by 字段1 排序方式1， 字段2 排序方式2\n```\n升序：asc，降序：desc\n### 分页查询\n语法：\n```plain text\nselect 字段列表 from 表名 limit 起始索引，查询记录数;\n```\n## **多表关系与操作**\n多表关系分为：一对一，一对多，多对多。\n- 一对多：在多的一方添加外键。\n- 多对多：创建一个中间表，中间表中有两方的外键。\n### **多表查询**\n### 概述\n当需要从多张表中查询数据并合并时，需要用到多表查询。当查询两张表时，各元素会按照笛卡尔积进行排列。可以使用where来排序不需要的笛卡尔积。\n```plain text\nselect * from tb_emp, tb_pst where tb_emp.job = tb_pst.id;\n```\n多表查询分为内连接，外连接和子链接。\n内连接查询AB交集部分数据。外连接分为左外连接与右外连接，左外连接查询左表所有数据与两张表交集部分数据，右外连接查询油表有关数据与交集部分数据。\n### 内连接\n内连接无法查询两表没有关联的部分。内连接的查询方法有两种，分别为隐式内连接与显示内连接：\n- 隐式`select 字段列表 from 表1，表2 where 条件`\n示例：\n```plain text\nselect tb_emp.name, tb_pst.pst_name from tb_emp, tb_pst where tb_emp.job = tb_pst.id;\n```\n- 显式`select 字段列表 from 表1 inner join 表2 on 条件`\n示例：\n```plain text\nselect tb_emp.name, tb_pst.pst_name from tb_emp inner join tb_pst on tb_emp.job = tb_pst.id;\n```\n可以给表起别名以简化代码：\n```plain text\nselect * from tb_emp e, tb_pst p where e.job = p.id;\n```\n### 外连接\n外连接包含左外连接和右外连接。\n左外连接会完全包含左表的数据，即使数据与右表没有关系。右外连接同理。\n- 左外连接：`select * from tb1 left outer join tb2 on conditions`\n示例：\n```plain text\nselect * from tb_emp left outer join tb_pst on tb_emp.job = tb_pst.id;\n```\n- 右外连接：`select * from tb1 right outer join tb2 on conditions`\n示例：\n```plain text\nselect * from tb_emp right join tb_pst tp on tp.id = tb_emp.job;\n```\n### 子查询\n- 标量查询：查询一个单行单列的值。\n```plain text\n# 例如，查询tb_emp表中，职位为管理员的员工\nselect tb_emp.name from tb_emp where job = (select id from tb_pst where tb_pst.pst_name = '管理员');\n\n# 查询在‘子墨’之后入职的员工\nselect tb_emp.name from tb_emp where entrydate > (select entrydate from tb_emp where name = '子墨');\n```\n- 列子查询：查询一列（可以多行）的值\n```plain text\n# 查询‘管理员’和'仓储人员'的所有人员信息\nselect * from tb_emp where job in (select id from tb_pst where pst_name = '管理员' or pst_name = '仓储人员');\n```\n需要注意必须使用表示范围的in操作符（而不是等于）\n### **案例**\n```plain text\n# 需求：\n# 1.查询价格低于200信用点的 菜品名称，价格 和 菜品的分类名称\n# 显式内连接方法\nselect drinks.name, drinks.price, category.name\nfrom drinks\n         inner join category on drinks.category_id = category.id\nwhere drinks.price < 200;\n\n# 隐式内连接方法\nselect drinks.name, category.name, drinks.price\nfrom drinks,\n     category\nwhere drinks.category_id = category.id\n  and drinks.price < 200;\n\n# 2.查询所有价格在 100信用点 - 300信用点 且 状态 起售 的菜品的名称 和 菜品的分类名称 （即使分类为空）\nselect drinks.name, drinks.price, category.name\nfrom drinks\n         left join category on drinks.category_id = category.id\nwhere drinks.price between 100 and 300 and drinks.status = 1;\n\n# 3.查询每个分类下最贵的菜品，展示出分类的名称，最贵的菜品价格\nselect category.name, max(drinks.price) as 'max price'\nfrom drinks,\n     category\nwhere drinks.category_id = category.id\ngroup by category.name;\n\n# 4.查询各个分类下 菜品状态为 起售，且 该分类下菜品总数大于等于3的 分类名称\nselect category.name, count(*)\nfrom category,\n     drinks\nwhere category.id = drinks.category_id\n  and drinks.status = 1\ngroup by category.name\nhaving count(*) >= 3;\n\n# 5.查询出 高级猫猫通行证套餐 中包含了哪些菜品，展示出 套餐名称，价格，包含菜品名称，价格，份数\nselect s.name, s.price, d.name, d.price, sd.copies\nfrom drinks d,\n     setmeal_drinks sd,\n     setmeal s\nwhere s.id = sd.meal_id\n  and d.id = sd.drink_id\n  and s.name = '高级猫猫通行证套餐';\n\n# 6. 查询出低于菜品平均价格的菜品信息（展示出菜品名称，菜品价格）\nselect drinks.name, drinks.price\nfrom drinks\nwhere drinks.price < (select avg(price) from drinks);\n```\n### **事务**\n删除两个互相关联的表，通常需要两条SQL语句，如果其中一条出错或执行失败，会导致一个表数据被删除，另一个表没有变化。而如果使用事务来组合两条语句，如果其中一条出错，整个事务都将执行失败。\n语法：\n```plain text\n# 开启事务\nstart transcation / begin;\n# 提交事务\ncommit;\n# 回滚事务\nrollback;\n```\n### 事务的四大特性\n- 原子性：事务是不可分割的最小单元，要不全部成功，要不全部失败。\n- 一致性：事务完成时，必须保证所有数据保持一致状态。\n- 隔离性：数据库系统提供的保护系统使得外部数据不受事务影响。\n- 持久性：一旦提交或回滚，事务对数据库中的数据修改是永久的。\n### **索引**\n索引是个b-二叉树，是一种数据结构，用来快速查找数据。\n- 创建索引\n\t在某表某字段上创建字段\n```plain text\ncreate index 索引名 on 表名(字段名);\n```\n- 查看索引\n```plain text\nshow index from 表名;\n```\n- 删除索引\n```plain text\ndrop index 索引名 on 表名;\n```",
  "Maven": "## **Maven项目组织格式**\n使用不同的编译器会导致不同的文件组织格式，使得同一个项目不能使用不同编译器编译，而maven提供了一份标准的文件组织格式。\n其标准文件格式如下：\n```plain text\nmaven-project\n    -src\n        -main             实际项目资源\n            -java        java源代码目录\n            -resources   配置文件目录\n        -test            测试项目资源\n            -java\n            -resources\n        pom.xml          项目配置文件\n```\n## **Maven坐标**\nmaven中的坐标是资源的唯一标识，通过该坐标可以唯一定位资源位置。使用坐标来定义项目或引入项目中需要的依赖。\n主要组成如下：\n```plain text\n<groupId>com.coffeebar</groupId>    //定义当前Maven项目隶属组织名称，通常为域名反写\n<artifactId>untitled</artifactId>   //定义当前Maven项目名称，通常是模块名称\n<version>1.0-SNAPSHOT</version>     //定义当前版本号\n```\n## **配置依赖**\n在maven中配置依赖需要在pom.xml中引入dependencies标签，如下：\n```plain text\n<dependencies>\n        <dependency>\n            <groupId>ch.qos.logback</groupId>\n            <artifactId>logback-classic</artifactId>\n            <version>1.2.3</version>\n        </dependency>\n</dependencies>\n```\ndependencies中包括了一对dependency标签。如果第一次使用某个依赖，在填写相应信息后可直接下载。依赖信息可以跳转 [Maven仓库](http://mvnrepository.com/)找到\n- 依赖具有传递性\n- 可以使用scope标签指定依赖的范围，如下：\n<table>\n<tr>\n<td>**scope值**</td>\n<td>**主程序**</td>\n<td>**测试程序**</td>\n<td>**打包（运行）**</td>\n</tr>\n<tr>\n<td>compile(default)</td>\n<td>Y</td>\n<td>Y</td>\n<td>Y</td>\n</tr>\n<tr>\n<td>test</td>\n<td></td>\n<td>Y</td>\n<td></td>\n</tr>\n<tr>\n<td>provided</td>\n<td>Y</td>\n<td>Y</td>\n<td></td>\n</tr>\n<tr>\n<td>runtime</td>\n<td></td>\n<td>Y</td>\n<td>Y</td>\n</tr>\n</table>\n## **生命周期**\nMaven包括三个生命周期：clean，default和site，在同一个生命周期内后面的生命阶段依赖于前面。\n- clean.clean：移除上一次构建生成的文件\n- default.compile：编译项目源代码\n- default.test：使用合适的测试框架并运行测试\n- default.package：将编译后的文件打包成jar，war等\n- default.install：安装项目到本地仓库\n可以在idea中执行maven脚本，也可以在命令行中输入mvn + 对应生命阶段执行生命阶段。",
  "SpringBoot": "## **SpringBootWeb入门**\n在以模板创建一个SpringBoot项目后，发现其src文件夹下已经有了main和test文件夹，且main文件夹中包含了入口文件。新建一个文件HelloController，此文件中包括了浏览器请求时返回的函数。\nHelloController.java:\n```plain text\n//这一行注解表示这个类是一个请求处理类\n@RestController\npublic class HelloController {\n    //这个注解表示这个函数是一个请求处理函数，且处理的是hello请求\n    @RequestMapping(\"/hello\")\n    public String hello() {\n        System.out.println(\"hello world\");\n        return \"Hello World\";\n    }\n}\n```\n点击运行，打开浏览器并输入localhost:8080/hello，即可看到HelloWorld消息。\n## **HTTP协议**\n### **请求数据格式**\nhttp请求的数据格式分为get和post请求，get请求的请求内容位于请求行内，有大小限制，但post位于请求体内，没有大小限制。\n```plain text\nGET /hello HTTP/1.1               请求行\nAccept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7\nAccept-Encoding: gzip, deflate, br\nAccept-Language: zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6\nCookie: Pycharm-962094d3=ae3baddc-2a20-4216-b8fa-e74dc567bffd\nHost: localhost:8080\n以上都是请求头\n\n如果是post还有下面的请求体：\n{ \"status\":1,\"name\":\"KringKoter\"}\n```\n### **请求响应格式**\n请求响应的数据也分为post和get，这与上面一致。\n```plain text\nHTTP/1.1 200                             响应行/1.1：协议版本，200：返回代码\nContent-Type: text/html;charset=UTF-8    响应头，形式是key:value\nContent-Length: 38\nDate: Tue, 26 Sep 2023 06:10:36 GMT\nKeep-Alive: timeout=60\nConnection: keep-alive\n\n如果是post，下面还有响应体\n```\n- 返回代码为以下形式：\n<table>\n<tr>\n<td>**代码**</td>\n<td>**状态信息**</td>\n</tr>\n<tr>\n<td>1xx</td>\n<td>响应中-临时响应码，表示请求已经接收，告诉客户端应该继续请求，如果完成，则忽略</td>\n</tr>\n<tr>\n<td>2xx</td>\n<td>成功</td>\n</tr>\n<tr>\n<td>3xx</td>\n<td>重定向-重定向到其他地方，让客户端再发起一次请求完成整个过程</td>\n</tr>\n<tr>\n<td>4xx</td>\n<td>客户端错误-处理发生错误，责任在客户端</td>\n</tr>\n<tr>\n<td>5xx</td>\n<td>服务器错误-处理发生错误，则仍在服务端</td>\n</tr>\n</table>\n- 常用的返回码：\n<table>\n<tr>\n<td>**状态码**</td>\n<td>**英文描述**</td>\n<td>**解释**</td>\n</tr>\n<tr>\n<td>200（！</td>\n<td>OK</td>\n<td>处理成功</td>\n</tr>\n<tr>\n<td>302</td>\n<td>Found</td>\n<td>表示所请求的资源已经移动到另一个url，浏览器会自动访问这个页面</td>\n</tr>\n<tr>\n<td>304</td>\n<td>Not Modified</td>\n<td>告诉客户端服务器的内容自上次请求后没变，直接用本地缓存即可</td>\n</tr>\n<tr>\n<td>400</td>\n<td>Bad Request</td>\n<td>客户端有语法错误，服务器无法理解</td>\n</tr>\n<tr>\n<td>404（！</td>\n<td>Not Found</td>\n<td>请求资源不存在，一般是url输入错误，或者网站资源被删除</td>\n</tr>\n<tr>\n<td>403</td>\n<td>Forbidden</td>\n<td>没有权限</td>\n</tr>\n<tr>\n<td>405</td>\n<td>Method Not Allowed</td>\n<td>请求方式有误，比如get误用了post</td>\n</tr>\n<tr>\n<td>428</td>\n<td>Precondition Required</td>\n<td>服务器要求有条件的请求，必须携带特定的请求头</td>\n</tr>\n<tr>\n<td>429</td>\n<td>Too Many Requests</td>\n<td>给服务器发送了太多的请求</td>\n</tr>\n<tr>\n<td>431</td>\n<td>Request Header Fields Too Large</td>\n<td>请求头太大，服务器不愿意处理请求</td>\n</tr>\n<tr>\n<td>500（！</td>\n<td>Internal Server Error</td>\n<td>服务器崩了</td>\n</tr>\n<tr>\n<td>503</td>\n<td>Service Unavailable</td>\n<td>服务器尚未准备好，服务器刚刚启动，还未初始化很好</td>\n</tr>\n</table>\n- 常见的响应头：\n<table>\n<tr>\n<td>**字段**</td>\n<td>**信息**</td>\n</tr>\n<tr>\n<td>Content-Type</td>\n<td>表示响应内容的类型，例如text/html，application/json</td>\n</tr>\n<tr>\n<td>Content-Length</td>\n<td>表示该响应内容的长度（字节数）</td>\n</tr>\n<tr>\n<td>Content-Encoding</td>\n<td>表示响应压缩算法，比如gzip</td>\n</tr>\n<tr>\n<td>Cache-Control</td>\n<td>指示客户端如何缓存，例如max-age=300表示最多缓存300秒</td>\n</tr>\n<tr>\n<td>Set-Cookie</td>\n<td>告诉浏览器为当前页面所在的区域设置cookie</td>\n</tr>\n</table>\n## **web服务器**\nweb服务器对http协议操作进行封装，简化web开发。且能部署web项目，对外提供网上信息浏览服务。\n- Tomcat\n这是一个轻量级web开源服务器，支持servlet，jsp等少量java ee规范。也被称为web容器，servlet容器。\n## **Tomcat的基本使用**\nTomcat是一个绿色软件，直接解压后，找到bin目录里的start.bat即可运行。若出现乱码，需要到logging配置文件里将UTF-8改为GDK。若启动后无法运行，需要配置环境变量JAVA_HOME至java安装路径。\nTomcat启动后，默认占用端口8080，可以在conf/server.xml中修改端口号。\n将带有index.html的文件放在webapps中即可完成部署。\n## **请求参数**\n### **请求简单参数**\n直接将参数名写在函数的参数列表即可。可以使用@RequestParam注解来指定请求的参数名，require标签指定了是否必须（默认为true）\n```plain text\n@RequestMapping(\"/simplePojo\")\n    public String simplePojo(@RequestParam(name = \"uname\", require = false)String username, int id) {\n        System.out.println(name + \":\" + id);\n        return \"OK\";\n}\n```\n### **请求实体参数**\n将对象名写在参数列表即可。\n```plain text\n@RequestMapping(\"/complexPojo\")\n    public String complexPojo(User user) {\n        System.out.println(user);\n        return \"OK\";\n}\n```\n### **请求数组/集合参数**\n需要用@RequestParam指定参数为List\\<\\>\n```plain text\n@RequestMapping(\"/listParam\")\n    public String listParam(@RequestParam List<String> name) {\n        System.out.println(name);\n        return \"OK\";\n}\n```\n### **请求时间参数**\n时间参数将封装在一个LocalDate或LocalDateTime对象中，需要使用@RequestParam(patter=\"\")来指定时间格式，注意请求的参数名必须和变量名一致，另外还需要注意日期标准格式是\"yyyy-MM-dd HH\\:mm\\:ss\"，M和H需要大写，否则传递参数不识别。\n```plain text\n@RequestMapping(\"/time\")\n    public String getTime(@DateTimeFormat(pattern = \"yyyy-MM-dd HH:mm:ss\")LocalDateTime localDateTime) {\n        System.out.println(localDateTime);\n        return \"OK\";\n}\n```\n### **请求json参数**\njson参数需要封装在一个对象中，这个对象可以自定义。需要在对象前加上@RequestBody注解来声明这个一个json参数。\n```plain text\n@RequestMapping(\"/json\")\n    public String getJson(@RequestBody User user) {\n        System.out.println(user);\n        return \"OK\";\n}\n```\n传递的json内的参量key需要与对象中的属性名相同，例如：\n```plain text\n{\n    \"name\":\"JSKringkoter\",\n    \"id\":3,\n    \"address\":{\n        \"city\":\"EHIS\",\n        \"street\":\"EHIS-4\"\n    }\n}\n```\n```plain text\npublic class Address {\n    public String city;\n    public String street;\n}\n\npublic class User {\n    public String name;\n    public int id;\n    public Address address;\n}\n```\n### **路径参数**\n可以在@RequestMapping的路径中使用\\{id\\}来动态获取路径参数。在函数的参量中，使用@PathVariable来指定后面的参量是一个路径参数。\n```plain text\n@RequestMapping(\"/path/{id}\")\n    public String pathParam(@PathVariable Integer id) {\n        System.out.println(id);\n        return \"OK\";\n    }\n}\n```\n也可以指定多个参数。\n```plain text\n@RequestMapping(\"/{name}/{id}\")\n    public String pathParam(@PathVariable String name, @PathVariable Integer id) {\n        System.out.println(name + \":\" + id);\n        return \"OK\";\n    }\n}\n```\n### **统一响应结果**\n可以使用一个Result来统一响应结果。Result类可以使用静态方法来简化创建流程，其包含了一个无参构造函数，一个有参构造函数，两个success工厂函数，一个error工厂函数。Result类中code数据表明了传递信息的id，message表示信息，data则是响应的数据。大致定义如下：\n```plain text\npublic class Result {\n    private Integer code;\n    private String message;\n    private Object data;\n\n    public Result() {\n    }\n\n    public Result(int code, String message, Object data) {\n        this.code = code;\n        this.message = message;\n        this.data = data;\n    }\n\n    public static Result success() {\n        return new Result(1, \"success\", null);\n    }\n\n    public static Result success(Object data) {\n        return new Result(1, \"success\", data);\n    }\n\n    public static Result error() {\n        return new Result(0, \"error\", null);\n    }\n    public int getCode() {\n        return code;\n    }\n\n    public void setCode(int code) {\n        this.code = code;\n    }\n\n    public String getMessage() {\n        return message;\n    }\n\n    public void setMessage(String message) {\n        this.message = message;\n    }\n\n    public Object getData() {\n        return data;\n    }\n\n    public void setData(Object data) {\n        this.data = data;\n    }\n\n    @Override\n    public String toString() {\n        return \"Result{\" +\n                \"code=\" + code +\n                \", message='\" + message + '\\'' +\n                \", data=\" + data +\n                '}';\n    }\n}\n\n```\n对应的响应方法如下：\n```plain text\n@RequestMapping(\"/ello\")\n    public Result hello() {\n        return new Result(1, \"helloworld\", null);\n}\n\n @RequestMapping(\"/result\")\n    public Result getResult() {\n        Address addr = new Address();\n        addr.setCity(\"EHIS\");\n        addr.setStreet(\"EHIS-4\");\n\n        return Result.success(addr);\n}\n```\n### **@RestController**\n@RestController是@Controller、@ResponseBody的组合体。@ResonseBody可以作用在@Controller类或者方法上，作用是直接将方法的返回值作为响应参数",
  "Vue/Element-UI": "## **项目文件的组成结构**\npublic文件夹包括发布的内容；\npublic/index.html 网站发布页；\nsrc文件夹包括源代码文件（重要；\nsrc/App.vue 入口vue文件，是重点需要修改的文件，代码修改主要在这里；\nsrc/main.js 入口js文件，可以不用动。\n## **Element组件库**\n来自饿了么团队倾情开发！\n安装组件库：\n```plain text\nnpm install element-ui@2.15.3 (版本号)\n```\n在view文件夹中添加组件，文件后缀为.vue，主要需要使用驼峰命名法。其基本代码如下：\n```plain text\n<template>\n    <div>\n        这里放需要显示的东西\n    </div>\n</template>\n\n<script>\nexport default {\n}\n</script>\n\n<style>\n</style>\n```\n新建组件完成后，需要在main.js中引入ElementUi包，在全局区域复制以下代码：\n```plain text\n//引入elementui\nimport ElementUI from 'element-ui';\nimport 'element-ui/lib/theme-chalk/index.css';\nVue.use(ElementUI);\n```\n如果需要显示，则要在App.vue中需要显示的地方引入，安装了vscode element helper插件之后之间使用\\<\\>括起需要引入的文件名即可。",
  "HTML&JavaScript": "## **标签**\n### **标题标签**\n```plain text\n<h1></h1>\n...\n<h6></h6>   重要程度依次降低\n```\n### **水平线标签**\n```plain text\n<hr>\n```\n### **图片标签**\n```plain text\n<img src=\"图片路径\" width=\"图片宽度\" height=\"图片长度\">\n```\n图片路径分为绝对磁盘路径，绝对网络路径和相对磁盘路径。在相对磁盘路径中，./表示当前目录，../表示上一级目录。\n当width或height只指定了一个时，另一个会按照比例缩放。\n### **注释**\n```plain text\n<!-- 注释正文 -->\n```\n在html中，任何文本都能被直接识别\n### **Span**\nspan标签是一个没有任何语义的布局标签，但可以用它来设置字体样式\n```plain text\n<span style=\"...\">Coffee Bar</span>\n```\n\n### **超链接**\n```plain text\n<a url=\"\" target=\"_self\">\n\n</a>\n```\nurl中为连接，target中可选_self与 _blank，前者为打开在当前页面，后者为新页面打开，默认为前者。\n### **音频，视频**\n```plain text\n<audio src=\"audio.mp4\" controls=\"controls\"></audio>\n\n<video src=\"video.mp4\" controls=\"controls\" width=400></video>\n```\n### **换行，段落**\n```plain text\n<br>\n<p>\n    ...\n</p>\n```\n### **空格：&nbsp**\n### **表格**\n```plain text\n<table border=\"0px\" cellspacing=\"1px\" width=\"600px\">\n    <tr>\n        <th>原神</th>\n        <th>明日方舟</th>\n    </tr>\n    <tr>\n        <td>米哈游</td>\n        <td>鹰角</td>\n    </tr>\n</table>\n```\n其中，table标签表示了这是一个表格，border表示边框线粗细，cellspacing表示单元格之间的空隙，width表示表格宽度。\ntr表示行，th表示表头，在表头中的元素自动加粗与居中，td表示单元格。\n### **表单**\n```plain text\n<form action=\"\" method=\"get / post\">\n\n</form>\n```\n在表单中，action表示提交内容的去向，method表示提交的方式，get表示向当前页面提交（默认），表单数据拼接在url后面，post表示向网络中提交，表单数据在请求体中携带，大小没有限制。\n表单项必须要有name属性，否则无法提交\n## **CSS格式控制**\n### **引入**\n- 行内引入\n```plain text\n<h1 style:\"...\">\n\n</h1>\n```\n- 内嵌引入\n内嵌引入需要在head部分进行声明，格式为：\n```plain text\n<style>\n    h1 {\n        ...\n    }\n    h2 {\n        ...\n    }\n</style>\n```\n- CSS文件引入（外联引入）\n这种引入方式需要一个.css文件来保存格式信息。\n```plain text\nh1 {\n    ...\n}\nh2 {\n    ...\n}\n```\n同时在html中使用link来引入，内嵌引入的优先级大于CSS文件引入。\n```plain text\n<link rel=\"stylesheet\" href=\"style.css\">\n```\n## **CSS选择器**\n- 元素选择器：\n```plain text\ntypename {\n}\n```\n- 类选择器：\n```plain text\n.classname {\n}\n```\n- id选择器：\n```plain text\n#id {\n}\n```\n其中，id选择器的优先级最高，类选择器次之，元素选择器最低\n## **CSS属性**\n- color：设置文本颜色\n- font-size：字体大小，单位为px\n- text-decoration：规定添加到文本的修饰，none表示定义标准的文本\n- line-height：行高\n- text-indent：定义第一个行内容的缩进\n- text-align：规定元素中文本的对齐方式，right.center.left\n### **盒子属性**\n组成：content·内容，padding·内边距，border·边框，margin·外边框\n属性：width：设置宽度  height：设置高度  border：设置边框的属性（边框粗细， solid， 颜色）<br>padding·内边距  margin：外边距\n如果只需要设置某个方位的边框，内边框或外边距，可以在属性名后加上-位置，如padding-top, padding-left, padding-right\n## **JavaScript基础**\n### **javascript的引入**\n在html中引入js有两中方法，一是内嵌引入，在html中使用\n```plain text\n<script>\n    alert(\"牛魔酬宾\")\n</script>\n```\n第二种是有独立的js文件，通过\n```plain text\n<script src=\"../js.js\"></script>\n```\n来引入。\n### **输出语句**\n- 在浏览器中输出：window.alert()\n- 将字符写入html，并在网页中显示：document.write()\n- 在网页控制台中输出：console.log()\n### **数据类型**\njs中使用var声明变量，且js是一门弱类型语言。var定义的变量是全局变量，可以重复定义。\n使用let声明的变量是局部变量，不能重复定义。可以使用const声明只读变量。\njs拥有五种数据类型：number, string, boolean, null, undefined。\n当数据未作初始化时，默认为undefined；使用typeof可以获取数据类型。\n使用=或进行比较操作，前者不会进行类型转换，而后者会进行类型转换，只要值相等就会判定为等。\n### **类型转换**\n将字符串转化为数字时，若有不是数字的元素，则转化为NaN，否则转化为数字。\n将字符串转化为boolean时，空字符串转化为false，只要有元素，则转化为true。数字转为boolean，0和NaN转化为false。Null和undefined均转化为false。\n全局方法parseInt()可将字符串类型转化为int（float需要用parseFloat()）。\n### **函数**\nJs是弱类型语言，不需要指定返回类型和参数类型。使用function关键字来声明函数。\n```plain text\nfunction(num1, num2) {\n    return num1 + num2;\n}\n```\n可以使用以下语法来声明一个函数对象：\n```plain text\nvar plus = function(num1, num2) {\n    return num1 + num2;\n}\n```\n### **对象**\n- Array对象\n定义Array对象可以使用如下语法：\n```plain text\nvar array = new Array(1, 2, 3, 4);\nvar array = [1, 2, 3, 4];\n```\n访问则使用\\[\\]符号。\n- String对象\n定义String对象使用与Array对象类似的语法。\nString对象有以下方法（成员方法）：\n```plain text\ncharAt()     //返回在指定位置的字符\nindexOf()    //检索字符串\ntrim()       //去除字符串两边的空格\nsubstring()  //提取字符串两个指定的索引号之间的字符\n```\n- 自定义对象\n定义自定义对象使用如下语法\n```plain text\nvar classname = {\n    int: 100,\n    string: \"我不到啊\",\n    fun(color) {\n        return color;\n    }\n}\n```\n- JSON对象\n定义JSON对象使用以下语法：\n```plain text\nvar jsonObject = '{\"name\":\"JSKringKoter\", \"age\":20, \"addr\":[\"南京\", \"仪征\"]}';\n```\nJSON对象与JS对象的相互转化使用以下语法：\n```plain text\nvar jsObject = JSON.parse(jsonObject);\nvar jsonObject = JSON.stringify(jsObject);\n```\n- BOM对象\nBOM是将整个浏览器封装而成的一个对象，包括五个方面：Window窗口，Navigator浏览器对象，Screen屏幕对象，History历史记录对象，Locatiion地址栏对象。\n对于Window对象，它的属性包括：\n```plain text\nwindow.history    对History对象的只读引用\nwindow.location   对location对象的只读引用\nwindow.navigator  对Navigator对象的只读引用\n```\n方法：\n```plain text\nalert()     弹出一个消息框，其中可以填消息内容\nconfirm()   确认框，可以填内容\nsetInterval()    按照指定时间循环执行函数，参数是一个函数对象和一个数字\nsetTimeout()     在指定的时间延时后执行操作，参数同上\n```\n其中，location拥有一个成员href，对这个成员赋值即可跳转至相应的页面。\n```plain text\nalert(location.href);      //输出当前页面的href\nlocation.href = \"jskrigkoter.github.io\"  //跳转页面\n```\n- DOM对象\nDOM是将document封装起来的一个对象，其中包括了该HTML文件中的所有内容。使用以下函数可以获得相应目标的引用：\n```plain text\ndocument.getElementById(Id);                  //获得指定id的元素\ndocument.getElementsByName(name);             //获得指定name的元素数组\ndocument.getElementsByClassName(classname);   //获得指定classname的元素数组\ndocument.getElementsByTagName(tagname);       //获得指定tagname的元素数组\n```\n### **JavaScript事件**\n监听事件可以通过两种方式：\n```plain text\n<input type=\"reset\" onclick=\"msgBoxCareful\"></input>\n<script>\n    function onclick() {\n        alert(\"牛魔酬宾\");\n    }\n</script>\n```\n或：\n```plain text\n<input type=\"reset\" id=\"resetButtom\"></input>\n<script>\n    document.getElementById(\"resetButtom\").onclick = function() {\n        alert(\"牛魔酬宾\");\n    }\n</script>\n```\non+事件可以找到挺多事件。\n## **Vue框架**\nVue是一种数据-模型-数据的框架，需要从其官网下载vue.js，并通过scirpt标签引入：\n```plain text\n<script url=\"vue.js\"></script>\n```\nVue可以实现数据的接管，将代码层的数据与前端相绑定，使得数据能够动态变化。要使Vue接管区域，需要使用以下命令：\n```plain text\n<script>\n    new Vue ({\n        el: \"#app\",\n        data: {\n            message=\"Hello World\",\n            url=\"http://jskringkoter.github.io\"\n        }\n        methods: {\n            handle: function() {}\n        }\n    })\n</script>\n```\n### **Vue指令**\n带有v-前缀的字段称作Vue指令。常用的指令有：\n<table>\n<tr>\n<td>**指令**</td>\n<td>**作用**</td>\n</tr>\n<tr>\n<td>v-bind</td>\n<td>实现与目标元素的单项绑定</td>\n</tr>\n<tr>\n<td>v-model</td>\n<td>在input标签中，实现双向绑定</td>\n</tr>\n<tr>\n<td>v-if</td>\n<td>条件性的渲染某元素，判定与ture时渲染</td>\n</tr>\n<tr>\n<td>v-else-if</td>\n<td>条件性的渲染某元素，判定与ture时渲染</td>\n</tr>\n<tr>\n<td>v-else</td>\n<td>条件性的渲染某元素，判定与ture时渲染</td>\n</tr>\n<tr>\n<td>v-show</td>\n<td>根据条件展示某元素，区别在于切换的是display的值</td>\n</tr>\n</table>",
  "Java": "## **一、基础部分**\n### **程序设计结构**\n- main方法需要为public static void\n- 文件名需要和公共类的名字相同，且使用.java作为扩展名\n### **注释**\n- 可以通过/\\*\\* \\*\\*/来生成注释文档\n### **swith语句**\n- 可以将switch语句作为右值\n- 使用分号而不是break\n- case使用-\\>\n```plain text\npublic class Main {\n    public static void main(String[] args) {\n        int a = 0;\n        int b = switch (a) {\n                case 1 -> 10;\n                case 2 -> 20;\n                default -> 30;\n        }\n    }\n}\n```\n### **输入和输出**\n- 输出\njava使用System.out的方法print系列进行输出：\n```plain text\npublic class Main {\n    public static void main(String[] args) {\n        System.out.print(\"Hello World\");    //print打印一个对象\n        System.out.println(\"Hello World\");  //println打印一个对象并换行\n    }\n}\n```\n- 输入\n要想使用输入，必须包含包util\n```plain text\nimport java.util.*;\n```\n使用输入前需要创建一个Scanner实例，将该实例与输入流绑定，再使用该输入流进行读取操作：\n```plain text\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner cin = new Scanner(System.in);\n        int a = cin.next();\n        String b = cin.nextLine();\n    }\n}\n```\n以下是输入Scanner实例的方法：\n```plain text\nString cin.next()           //读取下一个单词的输入（空格为止\nString cin.nextLine()       //读取下一行的输入（换行符为止\nint cin.nextInt()           //读取下一个int\ndouble cin.nextDouble()     //读取下一个double\nboolean cin.hasNext()       //检测输入中是否还有单词\nboolean cin.hasNextInt()\nboolean cin.hasNextDouble()\n```\n\n### **数组**\nJava中的数组和C中的指针数组类似。\n```plain text\nint[] array = new int[10];    //初始化一个长度为10的数组，10可以替换为变量\nint[] array = { 1, 2, 3, 4 }; //以列表初始化数组\nnew int[] { 1, 2, 3, 4 };     //声明一个无名数组\narray = new int[] { 1, 2, 3, 4 };  //使用无名数组赋值\n```\n- 数组拷贝\n若使用等号拷贝数组，原数组和拷贝数组将指向同一块内存空间，意味着对其中一个的修改也会影响另一个：\n```plain text\nint[] a = { 1, 2, 3 };\nint[] b = a;\nb[0] = 2;    //a[0]也被修改了\n```\n可以使用Arrays.copyOf()来拷贝到新的数组中：\n```plain text\nint[] a = { 1, 2, 3, 4 };\nint[] b = Arrays.copyOf(a, 4);   //第一个参数为要拷贝的数组，第二个是拷贝长度。\n\n//可以通过这种方法增加数组长度\nb = Arrays.copyOf(b, 2 * b.lenth);\n```\n- 数组排序\n可以使用Arrays.sort()对数组排序：\n```plain text\nint[] a = { 2, 34, 12, 3, 1 };\nArrays.sort(a);\n```\n### **var字段**\n相当于auto，若可以从右边的表达式中推出变量类型，则可以使用var自动推断变量类型。多用于临时变量。\n### **null字段**\n可以将对象声明为null值，但如果对其使用方法，将会导致NullPointerException异常：\n```plain text\nPlayer a = null;\nString str = a.getName();   //抛出异常\n```\n## **二、面向对象部分**\n### **构造器**\n构造器是构造函数的另一种叫法。\n构造器无法使用C++风格的列表初始化。\n可以在类中加入代码块，在构造对象时这些代码块会调用：\n```plain text\npublic class Player {\n    public int ID;\n\n    {\n        this.ID = Math.random();\n    }\n\n    public Player(int ID)\n    {\n        this.ID = ID;\n    }\n}\n```\nJava中没有析构器，它能够自动完成垃圾回收。\n### **方法**\nJava中的实现不能放置于类外。放置于类内并不意味着声明为内联。若遇到同名问题，Java会发生错误，而不是同名遮掩机制。\n- main方法\n每一个类都可以有一个main方法，可以在其中包含示例代码，单独运行该类文件时就可以运行该代码。\n### **继承**\nJava中没有private和protected继承，仅有public继承，其语法如下：\n```plain text\npublic class A extends B {\n\n}\n```\n- 在子类中使用超类方法\n在子类中调用超类方法需要用到关键字super：\n```plain text\npublic class A extends B {\n    private String name;\n\n    public A(String str1, String str2)\n    {\n        super(str2);                    //super调用基类构造器\n        this.name = str1;\n    }\n\n    public String get()\n    {\n        String baseStr = super.get();   //super调用基类方法\n        return name + baseStr;\n    }\n}\n```\n- 多态\nJava中默认方法为virtual的，若不需要为虚，则使用关键字final。\n- instanceof运算符\n用于检查对象是否为某类的实例。\n### **Object超类**\n在java中，所有类型都继承自Object超类，这是一个功能很强大的类，所有子类都可以使用其方法（如equals。\n- equals方法\nequals方法有两个版本，一是成员函数版本，二是静态方法版本。\n```plain text\npublic class Main {\n    public static void main {\n        Player pl = new Player(\"KringKoter\");\n        Player p2 = new Player(\"Afternoon Tea\");\n\n        System.out.print(p1.equals(p2));             //成员函数版本\n        System.out.print(Objects.equals(p1, p2));    //静态方法版本（注意是Objects\n\n    }\n}\n```\n- toString方法\n此方法将类转换为字符串。当遇到对象+字符串时，将自动调用该对象的toString方法。\n每个类都需要覆写一次toString方法，如果不进行覆写， 调用时将生成类名+散列码的字符串。\n- hashcode方法\n为对象生成一个散列码。\n- getClass方法\n返回一个类对象，保存了有关对象的信息。\n### **ArrayList**\n类似于vector容器。ArrayList可以使用菱形语法：\n```plain text\npublic class Main {\n    public static void main(String[] args) [\n        ArrayList<int> array1 = new ArrayList<>();      //菱形语法\n        var array2 = new ArrayList<int>();              //使用var自动类型推断\n        ArrayList<int> array3 = new ArrayList<>(100);   //指定初始长度\n    ]\n}\n```\n### **包**\n- 导入包\n使用import来导入包和包中的所有类，其中，.\\*只能导入一个包，不能同时导入例如java为前缀的所有包：\n```plain text\nimport java.lang.System;         //导入System包\nimport java.util.*;              //导入util包\nimport java.*.*;                 //不能这样写\n```\n在两个包中有同名成员时，可以显示指出使用哪一个包的成员：\n```plain text\nimport java.util.*;\nimport java.sql.*;        //两个包中都有Date类\n\nimport java.util.Date;    //显式指出使用util包的Date类\n```\n如果两个包的同名成员都需要使用，则在使用中加上相应的前缀：\n```plain text\nvar startTime = new java.util.Date();\nvar today = new java.sql.Date();\n```\n- 在包中添加类\n若需要在包中添加类，需要将包名放在类的开头。包名使用一个因特网域名的倒序（如Player.com变为com.Player）。如果未在包中放置package语句，则包为无名包。\n以下是Player.java的包声明语法，其文件放置于com\\\\Player\\\\corejava中：\n```plain text\npackage com.Player.corejava\n```\n### **对象包装**\n有的时候需要将int这样的基本类型变为对象类型，所有基本类型都有一个与之对应的类。包装器类不可变，不能修改其中的数据，且不能派生其子类。诸如ArrayList的容器类无法使用基本类型，此时可以使用包装器类将基本类型包装：\n```plain text\nArrayList<int> array = new ArrayList<>();      //非法\nArrayList<Integer> array = new ArrayList<>();  //合法\n```\n可以直接传递基本类型给array，此时基本类型将自动包装为对象类型；反之，对象类型可以直接赋值给基本类型，称之自动拆箱。\n```plain text\narray.add(1);          //自动装箱\nint i = array.get(2);  //自动拆箱\n```\n由于java中==运算符比较的是两者是否指向同一片内存空间，对于两个相同包装器对象的比较很有可能失败。此时需要使用equals方法：\n```plain text\nInteger int1 = 1000;\nInteger int2 = 1000;\nif (int1 == int2) return 1;      //不能这样比较\nif (int1.equals(int2)) return 2; //应该这样比较\n```\n### **可变参数个数**\n使用...语法可以提供多个且数量不定的参数：\n```plain text\npublic class Main {\n    public static int func(int... value) {\n        int result = 0;\n        for (var i : value)\n            result += i;\n        return result;\n    }\n}\n```\n### **抽象类和抽象方法**\n使用关键字abstract。包含一个或多个抽象方法的类需要声明为抽象。\n```plain text\npublic abstract class Super {\n    public abstract void get();\n}\n```\n### **Lambda表达式**\n基本语法：\n```plain text\n(String fisrt, String second) -> first.length() - second.length()\n(参数列表)                     -> 表达式体\n```\n如果没有大括号，无需指定返回类型。Java会自动推断返回类型。如果使用了大括号，则需要手动返回值，且不能使用单一情况的表达式：\n```plain text\n(String first, String second) -> {\n    if (first.length() - first.length() > 0)\n        return 1;\n    else\n        return 2;\n}\n```\n## **三、接口**\n### **声明接口**\n接口是对希望符合接口的类的一组需求，如果需要使用某方法，意味着类必须实现某接口。如果要使用Arrays类中的sort函数，则需要实现Comparable接口。\n```plain text\npublic interface Compareable<T> {\n    int compareTo(T t);\n}\n```\n接口里的方法不提供实现，且自动声明为public。要声明一个类实现某接口，需要将类声明为实现给定的接口，且对接口中的方法提供定义。要声明一个类实现某接口，需要使用关键字implement：\n```plain text\npublic class Player implements Complarable<Player> {\n    private int level;\n\n    public int compareTo(Player player) {\n        return Intager.compare(this.level, player.level);\n    }\n}\n```\n### **接口的属性**\n接口不是类，不能使用new来声明一个接口，但是可以将一个接口变量与实现了这个接口的一个具体对象绑定。\n接口也有继承体系，可以使用extends关键字拓展接口，使其从泛用性强拓展到专用性强：\n```plain text\npublic interface A {\n    void func1();\n}\n\npublic interface B extends A {\n    void func2();\n}\n\npublic interface C extends B {\n    int level;\n    void func3();\n}\n```\n接口中不能包含实例字段，但可以包含常量。在接口内的实例字段自动声明为public static final。\n可以将接口方法显式标记为public，将实例字段标记为public static final，但java语言规范建议不要使用冗余的关键字。\n### **多个接口**\nJava没有MI，但是可以实现多个接口。在接口中使用逗号隔开：\n```plain text\npublic class Player implements A, B {\n    ...\n}\n```\n### **默认方法**\n可以在接口中使用default来提供默认方法，这样实现接口的类可以不提供其实现：\n```plain text\npublic interface A<T> {\n    default int compare(T t) {\n        return 0;\n    }\n}\n```\n### **默认方法冲突**\n如果同时实现了两个接口，且这两个接口内有签名一致的默认方法，需要手动指定使用哪一个方法：\n```plain text\ninterface Compare1<T> {\n    default int compare(T t) {\n        return 0;\n    }\n}\n\ninterface Compare2<T> {\n     default int compare(T t) {\n         return 1;\n     }\n}\n\npublic class Player implements Compare2<Player>, Compare1<Player> {\n    public int compare(Player player) {\n        return Compare1.super.compare(player);  //手动指定使用Compare1的compare方法\n    }\n}\n```\n## **四、泛型和集合**\n### **声明泛型类**\nJava中无需使用template来指定泛型：\n```plain text\npublic class name<T> {\n    private T element;\n\n    public name() { element = null; }\n    public name(T element) {\n        this.element = element;\n    }\n\n    public T getElement() {\n        return this.element;\n    }\n\n    public void setElement(T elemnet) {\n        this.element = element;\n    }\n}\n```\n### **声明泛型方法**\n泛型方法可以在普通类中声明，也可以在泛型类中声明。\n```plain text\npublic class name {\n    public <T> T function() {\n        return T;\n    }\n}\n```\n### **集合和Collection接口**\nJava中的集合类似于STL，包括了ArrayList，SortedMap，set等容器。这些容器都实现自Collection接口（SortedMap实现自Map接口）。要想声明自己的数集，并使用相应的算法，需要实现Collection接口：\n```plain text\npublic class MySet<E> implements Collection<E> {\n    public int size() {\n        reutrn size;\n    }\n\n    public boolean isEmpty {\n        return size == 0;\n    }\n\n    ...\n}\n```\n如果不是确实需要实现自己的集合，Java库中的现有集合就能满足需求。Collection接口中的具体方法见《Java核心技术·卷一》383页。\n### **迭代器**\nJava中的迭代器与C++中的不同，它并不是类似于数组索引的存在，而是相间于两个元素之间。\n```plain text\npublic class Main {\n    public static void main(String[] args) {\n        ArrayList<Integer> array = new ArrayList<>();\n        for (int i = 0; i < 10; i++) {               //向集合中添加元素\n            array.add(i);\n        }\n\n        Interator<Integer> iter = array.interator(); //获得array的迭代器，该迭代器从首前开始。\n        for (iter.hasNext())                         //hasNext函数检查是否有下一个元素\n        {\n            int a = iter.next();                     //将迭代器移向下一个元素之后，并返回刚才的元素。\n            System.out.println(a);\n        }\n\n        itera.remove()                               //删除上次调用next的元素。\n\n    }\n}\n```\n有关迭代器的方法见《Java核心技术·卷一》394页。\n### **散列集（HashSet）**\n有一种众所周知的数据结构，可以用来快速查找对象，被称作散列表。散列表为每个对象计算一个散列码。要想查找每个元素在表中的位置，要先计算其散列码，再通过运算找到桶的编号。如果桶中已经有了元素，这种现象被称作散列冲突（Hash Collision），这时需要将新对象与原对象进行比较，查看这个数据是否存在。\nJava库提供了一个散列集，可以用add方法插入元素，使用contains方法查看元素是否存在。当访问元素时，会通过一种看起来随机的方式，只有不关心集合中元素顺序时才使用HashSet。\n这个实例程序读取一行字符，并打印其前20个不重复的单词。\n```plain text\npublic class Main {\n    public static void main(String[] args) {\n        Set<String> stringSet = new HashSet<>();\n\n        var cin = new Scanner(System.in);\n        while (cin.hasNext()) {\n            String temp = cin.nextLine();\n            stringSet.add(temp);\n        }\n\n        var iter = stringSet.iterator();\n        while (iter.hasNext()) {\n            String temp = iter.next();\n            System.out.println(temp);\n        }\n    }\n}\n```\n### **有序树集（TreeSet）**\n相较于HashSet，基于红黑树排序的树集在add数据时自动排序。添加数据时树集较慢，但查找较快。要使用树集，必须能够比较元素。这些元素必须实现了Comparable接口。\n下面的实例程序使用实现了Comparable接口的测试类Item，比较其中的number实例字段。\n```plain text\npublic class Main {\n    public static void main(String[] args) {\n        Set<Item> itemSet = new TreeSet<>();\n\n        for (int i = 0; i < 20; i++) {\n            Random random = new Random();\n            itemSet.add(new Item(random.nextInt(101)));\n        }\n\n        Iterator<Item> iter = itemSet.iterator();\n        while (iter.hasNext()) {\n            System.out.println(iter.next());\n        }\n    }\n}\n\npublic class Item implements Comparable<Item>{\n    private int number;\n\n    public Item(int number) {\n        this.number = number;\n    }\n\n    public int compareTo(Item item) {\n        return number - item.number;\n    }\n\n    @Override\n    public String toString() {\n        return \"Item{\" +\n                \"number=\" + number +\n                '}';\n    }\n}\n```\n### **映射（Map）**\n映射分为散列映射和树映射，区别在于散列为无序而树为有序，它们都实现了Map接口。使用put函数向映射中添加元素，使用get来获取元素。在使用get时，必须提供key，因此必须记住key。\n如果没有找到元素，则返回null，但null并不能包含有效信息，可以使用getOrDefault来使用自定义的默认值：\n```plain text\nMap<String, String> map = new HashMap<>();\nint a = map.getOrDefault(\"wobuzhidao\", 100);    //a将为默认值100\n```\n可以使用forEach方法，该方法接受一个lambda表达式，映射中的元素会依次调用此表达式。\n```plain text\npublic class Main {\n    public static void main(String[] args) {\n        Map<Integer, String> stringMap = new HashMap<>();\n        stringMap.put(20040528, \"JSKringKoter\");\n        stringMap.put(31061231, \"FlyingDream\");\n        stringMap.put(31060825, \"Ou'yang Xingyu\");\n\n        stringMap.put(20040930, \"Afternoontea\");\n        stringMap.put(20040528, \"JSKring\");\n\n        stringMap.remove(2004);\n\n        stringMap.forEach((k, v) ->\n                System.out.println(\"key=\" + k + \" value=\" + v));\n    }\n}\n```\n有关映射的方法在《Java核心技术·卷一》408页。\n### **更新映射条目问题**\n如果需要更换一个条目，但不知道是否存在，需要进行检查：\n```plain text\nMap<String, Integer> counts = new HashMap<>();\ncounts.put(\"word\", counts.get(\"word\") + 1);\n```\n这些代码在word出现后将其引用计数+1，但如果word第一次出现，则会抛出NullPointerException异常。补救方法是，使用getOrDefault函数：\n```plain text\ncounts.put(\"word\", counts.getOrDefault(\"word\", 0) + 1);\n```\n### **链接散列集与映射**\nLinkedHashSet和LinkedHashMap会记住元素插入的顺序，使其看起来以一种有序的方法排序。\n### **排序与散排**\nCollections类实现了一组方法，可以将给定的集合排列或散排，前提是其实现了Comparable接口。\n- 排序：Collections.sort()\n- 散排：Collections.shuffle()\n```plain text\npublic class Main {\n    public static void main(String[] args) {\n        ArrayList<Integer> integerArrayList = new ArrayList<>();\n        for (int i = 0; i < 30; i++) {\n            integerArrayList.add(i);\n        }\n\n        Collections.shuffle(integerArrayList);\n        System.out.println(integerArrayList);\n        Collections.sort(integerArrayList);\n        System.out.println(integerArrayList);\n    }\n}\n```\nList接口也有一个sort函数，但其接受一个比较器对象。Comparator.reverseOrder函数返回一个将元素倒序排序的比较器。下面的代码将integerArrayLIst的元素按逆序排序。\n```plain text\nintegerArrayList.sort(Comparator.reverseOrder());\n```\n### **交集**\n使用retainAll函数可以快速生产交集，这个函数接受一个Collections，保留两者都有的元素，删除没有的元素。\n```plain text\nArrayList<Integer> oldList = new ArrayList<>();\nArrayList<Integer> newList = new ArrayList<>();\n\noldList.retainAll(newList);\n```\n## **五、图形用户界面程序设计**\n### **创建窗体**\nSwing类位于javax中，其被认为是一个拓展包。在每个Swing程序中，Swing组件必须由事件分派线程配置，它将鼠标点击和按键等事件传递给用户界面组件，下面的代码用来在事件分派线程中执行语句：\n```plain text\nEventQueue.invokeLater(() ->\n{\n    statements\n});\n```\n想要显示一个界面，需要先声明一个继承了JFrame的类。这个类包含了窗体的基本信息，如长度和高度。在类的构造器中，可以通过函数来设置构造时的基本属性：\n```plain text\nclass SimpleFrame extends JFrame {\n    static final int DEFAULT_WIDTH = 300;\n    static final int DEFAULT_HIGHT = 400;\n\n    public SimpleFrame() {\n        setSize(DEFAULT_WIDTH, DEFAULT_HIGHT);\n    }\n}\n```\n然后在主函数中创建这个类的对象，窗体对象创建后可以对其进行配置：\n```plain text\npublic static void main(String[] args) {\n    EventQueue.invokeLater(() -> {\n        var frame = new SimpleFrame();\n        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);  //设置退出窗体时程序即退出\n        frame.setLocation(100, 100);                           //设置窗体位置\n        frame.setResizble(true);                               //设置是否可以调整大小\n        frame.setVisible(true);                                //设置为可见\n    });\n}\n```\n- 属性\n如下的一组代码称作窗体的一个属性，属性由一个get和一个set组成（对于boolean类型，get为is），将get或set后的单词小写即为其属性：\n```plain text\npublic String getTitle()\npublic void setTitle(String title)\n```\n以及：\n```plain text\npublic boolean isResizable();\npublic void setResizble(boolean resizable)\n```\n- 调整合适的窗体大小\n要确定合适的窗体大小，需要先知道屏幕的大小。调用Toolkit类的静态方法getDefaultToolkit得到一个Toolkit对象，这个对象中的getScreenSize方法以Dimension对象保存屏幕大小信息。该Dimension对象中的height和width实例字段保存了高度的宽度信息。\n```plain text\nToolkit tool = Toolkit.getDefaultToolkit();\nDimension screen = tool.getScreenSize();\nint height = screen.height;\nint width = screen.width;\nframe.setSize(height / 2, width / 2);\n```\n此外，可以提供窗体图标：\n```plain text\nImage img = new ImageIcon(\"icon.gif\").getImage();\nsetIconImage(img);\n```\n### **添加组件**\n图形界面的组件被放在JComponent的类中，要创建自己的组件时，需要先创建一个继承了JComponent的类，并覆盖其中的paintComponent方法，该方法将一个Graphics对象作为参量：\n```plain text\nclass SimpleComponent extends JComponent {\n    public void paintComponent(Graphics g) {\n        g.drawString(\"我不知道\", 100, 100);\n    }\n}\n```\ndrawString方法用于打印字符串。\n### **处理图形**\n我们将使用Java 2D图形库绘制图形，需要获得Graphics2D的一个对象。这个类是Graphics的一个子类，要使用2D，需要进行强制类型转换：\n`Graphics2D g2 = (Graphics2D) g`\n可以使用Graphics2D对象的方法来处理图形。",
  "Linux": "### 前置：环境部署\n使用VMware虚拟化硬件。\n使用CentOS作为系统OS。\n## Linux命令\n命令就是Linux程序，执行命令就是调用linux程序处理问题。它的基本结构为：\n```markdown\ncommand [-options] [parameter]\n\ncommand 命令\n-options 命令的选项，可以控制命令的行为细节\nparameter 命令的参数，多数用于命令的指向目标\n```\n\n## LS命令\nls命令的作用是列出目录下的内容，语法为：\n```markdown\nls [-a -h -l] [路径]\n```\n不使用选项和参数时，该命令平铺列出当前工作目录下的内容\n<span color=\"yellow\">Linux系统启动时默认加载/home/username的目录，即\\~或HOME目录。该目录是每个Linux用户的个人账户目录。下面两条语句是等效的：</span>\n```markdown\ncd ~\ncd /home/username\n```\n- ls命令的参数和选项\n\t- `-a`：列出所有的文件，包含隐藏的文件和文件夹\n\t\t以`.`开头的文件和目录是Linux系统的隐藏文件\n\t- `-l`：以列表形式显示内容，并展示更多信息\n\t- `-h`：列出文件的大小，<span color=\"yellow\">必须和</span><span color=\"red\">`-l`</span><span color=\"yellow\">连用</span>\n\t<span color=\"blue\">参数可以连用，例如可以</span><span color=\"red\">`-la`</span>\n\n## CD命令和PWD命令\ncd命令的作用是切换目录，语法为：\n```markdown\ncd [Linux路径]\n```\ncd命令没有选项只有参数，当不提供路径时，将回到HOME目录。\npwd命令的作用是打印当前工作目录，直接使用，没有选项和参数。\n\n## 相对路径、绝对路径和特殊路径符\n- 绝对路径以斜杠`/`开头，相对路径不以斜杠开头\n- `.`表示当前目录\n- `..`表示上一级目录\n- `~`表示HOME目录",
  "汇编语言": "### 课余内容\n## DOSBOX debug 调试工具的常用指令\n- `-r`：可以显示CPU寄存器中的所有数据，并将CS:IP所指向的内容翻译成指令\n\t- `-r AX`：修改寄存器中的内容\n- `-d`：可以显示地址中的数据，默认显示128个字节\n\t- `-d 1000:0`：显示从1000:0开始的128个字节\n\t- `-d 1000:0 2F`：显示从1000\\:0到1000\\:2F的内容\n- `-u`：可以显示地址中的指令\n- `-a`：向内存地址中写入指令\n\t- `-a 2000:0`：在指定的内存中写入指令\n- `-t`：执行当前CS:IP中的指令\n- `-e`：可以修改内存中的内容\n\t- `-e 2000:0 “abcdefg” `：将内存中的内容修改为字符串对应的ASCⅡ码\n\n---\n### 学校内容\n# 第三章 汇编语言指令集\n## 3.3 寻址方式\n- 立即寻址：操作数包含在指令中，如`MOV AX, 1111`\n- 寄存器寻址：操作数存放在寄存器中，如`MOV AX, BX`\n- 存储器操作数寻址\n\t- 直接寻址\n\t\t- 段寄存器名称：\\[ 偏移地址 \\] \n\t\t\t如`MOV AX, DS:[50H]` ，数据保存在地址为DS:\\[50H\\]的内存中\n\t\t- 段寄存器名称：\\[ 变量名 \\]\n\t\t\t如`MOV AX, DS:BUF`，BUF是一个变量，数据保存在地址为DS:BUF的内存中\n\t- 间接寻址\n\t\t段寄存器名称：\\[ 间址寄存器 \\]\n\t\t如`MOV AX, DS:[BX]`，BX称为间址寄存器\n\t- 基址寻址\n\t\t段寄存器名称：\\[ 基址寄存器+位移量 \\]\n\t\t如`MOV AX, DS:[BX+3]`\n\t\t<span color=\"yellow\">基址寄存器只有BX和BP可以使用</span>\n\t- 变址寻址\n\t\t段寄存器名称：\\[ 变址寄存器 + 位移量 \\]\n\t\t如 `MOV AX, SS:[SI + 3]`\n\t\t<span color=\"yellow\">变址寄存器只有SI和DI可以使用</span>\n\t- 基址变址寻址\n\t\t基+变就是两个加起来用\n\t在实际书写中，DS：或SS：可省略。数据段寄存器默认使用DS段寄存器，堆栈段寄存器默认使用SS段寄存器。\n\t<table>\n<tr>\n<td>基址寄存器</td>\n<td>约定访问的逻辑段</td>\n<td>变址寄存器</td>\n<td>约定访问的逻辑段</td>\n</tr>\n<tr>\n<td>BP</td>\n<td>堆栈段</td>\n<td>SI、DI</td>\n<td>数据段</td>\n</tr>\n<tr>\n<td>BX</td>\n<td>数据段</td>\n<td></td>\n<td></td>\n</tr>\n\t</table>\n## 3.4 汇编语言语法\n### 3.4.1 汇编语言语句类型和格式\n<span color=\"blue\">**符号指令（机器指令）**</span>：符号指令是CPU直接执行的指令，是一种指令性语言。\n<span color=\"blue\">**伪指令**</span>：伪指令为汇编工具提供信息，是一种指示性语语言。伪指令的基本结构：\n<table>\n<colgroup>\n<col>\n<col>\n<col>\n<col width=\"265\">\n<col width=\"197\">\n</colgroup>\n<tr>\n<td>变量</td>\n<td>伪指令助记符</td>\n<td>空格</td>\n<td>操作数项，多个操作数间用逗号隔开</td>\n<td>; 注释</td>\n</tr>\n</table>\n### 3.4.2 常用命令\n- <span color=\"blue\">SEG运算符</span>\n\tSEG运算符用来求一个逻辑段的段基址，或标号或变量所在逻辑段的段基址。\n\t`MOV AX, SEG DATA` 作用为取出`DATA`的段基址，并赋值给`AX`\n\t由于逻辑段段名本身就包含了段基址，所以可以省略SEG符号\n- <span color=\"blue\">OFFSET运算符</span>\n\tOFFSET运算符用来求一个逻辑段的偏移地址。如：\n\t```markdown\nMOV DB 10H, 20H, 30H\nMOV BX, OFFSET BUF\nMOV AH, [BX+1]\n\t```\n\t效果是取出BUF的偏移地址，放置在BX中，执行后AH=20H\n- <span color=\"blue\">\\$运算符</span>\n\t\\$运算符返回当前汇编地址计数器的值。通常使用它计算变量在逻辑段中占用的字节总数。如：\n\t```markdown\nBUF DB ‘HELLO WORLD’\nCOUNT EQU $-BUF\n\t```\n- <span color=\"blue\">PTR运算符</span>\n\t临时类型转换运算符。\n### 3.4.3 操作项\n- <span color=\"blue\">DB型数据</span>\n\tDB（Define Byte）表示字节型定义。它定义的常量按照字节顺序排列。它可以定义用单引号引起的字符串，字符串中的字符依次排列在内存中。\n\t```markdown\nNUM1 DB 12H, 64, -1, 3*3\n\t\t DB 11H        ;可以跨越多行定义\n\t\t DB 'HELLO'    ;可以定义用单引号引起的字符串\nNUM2 DB ?, ?       ;问号代表随机数\n\t```\n\t以上定义的内容在内存中的排列和C语言中的数组类似，先定义的字节位于低位字节中。\n\t<table>\n\t<colgroup>\n\t<col width=\"119\">\n\t<col width=\"120\">\n\t</colgroup>\n<tr>\n<td>NUM1</td>\n<td>12H</td>\n</tr>\n<tr>\n<td></td>\n<td>40H</td>\n</tr>\n<tr>\n<td></td>\n<td>FFH</td>\n</tr>\n<tr>\n<td></td>\n<td>09H</td>\n</tr>\n<tr>\n<td></td>\n<td>11H</td>\n</tr>\n<tr>\n<td></td>\n<td>'H’</td>\n</tr>\n<tr>\n<td></td>\n<td>‘E’</td>\n</tr>\n<tr>\n<td></td>\n<td>‘L’</td>\n</tr>\n<tr>\n<td></td>\n<td>‘L’</td>\n</tr>\n<tr>\n<td></td>\n<td>‘O’</td>\n</tr>\n<tr>\n<td>NUM2</td>\n<td>XXH</td>\n</tr>\n<tr>\n<td></td>\n<td>XXH</td>\n</tr>\n\t</table>\n- <span color=\"blue\">DW型数据</span>\n\tDW（Define Word）表示字型数据。它定义的变量占用两个字节，若定义时只定义了一个字节，则另一个字节将用’00H’补全。DW型数据的字符型常数只能是一个或两个字符。\n\t```markdown\nNUM1 DW 1234H, 56, 'AB', 'C', ?\n\t```\n\t以上定义的每个字（两个字节）中，后面的字节排列在低位字节上。\n\t<table>\n<tr>\n<td>NUM1</td>\n<td>34H</td>\n</tr>\n<tr>\n<td></td>\n<td>12H</td>\n</tr>\n<tr>\n<td></td>\n<td>56H</td>\n</tr>\n<tr>\n<td></td>\n<td>00H</td>\n</tr>\n<tr>\n<td></td>\n<td>'A‘</td>\n</tr>\n<tr>\n<td></td>\n<td>'B’</td>\n</tr>\n<tr>\n<td></td>\n<td>‘C‘</td>\n</tr>\n<tr>\n<td></td>\n<td>00H</td>\n</tr>\n<tr>\n<td></td>\n<td>XXH</td>\n</tr>\n<tr>\n<td></td>\n<td>XXH</td>\n</tr>\n\t</table>\n- <span color=\"blue\">DD型数据</span>\n\tDD（Define Double Word）表示双字定义。它定义两个字，也就是四个字节。\n\t定义的每个双字中，后面的字节依然排列在低位字节上\n## 3.5 汇编语言基本指令集\n### 3.5.1 传送类指令\n- <span color=\"blue\">数据传送指令</span>\n\t`MOV 目标, 源操作数`\n\t将源操作数传送到目标操作数中\n\t变体：\n\t- MOVSX符号位拓展传送指令，缺失的位数用符号补齐\n\t- MOVZX零拓展传送指令，缺失的位数用零补齐\n- <span color=\"blue\">偏移地址传送指令</span>\n\t`LEV 目标, 源操作数`\n\t将源操作数的偏移地址传送到目标操作数\n\t`LEV AX, BUF`等价于`MOV AX, OFFSET BUF`\n- <span color=\"blue\">交换指令</span>\n\t`XCHG 目标操作数, 源操作数`\n\t将源操作数和目标操作数的数据交换，需要字长相等\n对于双操作数指令，需要注意以下几点：\n- <span color=\"green\">立即数不能作为目标操作数</span>\n- <span color=\"green\">源、目标操作数不能同时为内存数</span>\n- <span color=\"green\">不允许将立即数直接传送到段寄存器，需要寄存器中继</span>\n\t```plain text\nMOV AX, 1000H\nMOV DS, AX\n\t```\n- <span color=\"green\">源、目标操作数要等长，必要时使用PTR运算符临时修改</span>\n- <span color=\"green\">基址+变址寻址中，寄存器写法顺序不同，默认的段寄存器不同</span>\n\t```plain text\nSS:[BP + SI]\nDS:[SI + BP]\n\t```",
  "离散数学：数理逻辑": "> 这玩意就是和大学物理并列的两坨。\n---\n# 第1章 命题逻辑的基本概念\n## 1.1 命题与连接词\n- 命题是指一个判断句的语义（实际表达的概念），这个概念是可以被定义并观察的现象。命题不是指判断句本身，而是指所表达的语义。\n- 命题的真值：作为命题的陈述句所表示的判断结果。\n\t命题的真值只会有两个结果：真 或 假。真值为真为真命题，反之为假命题。\n- 不能被分解成更简单的命题的命题称作**简单命题/原子命题**。由简单命题复合成的命题称作**符合命题**\n- 悖论：由假推出真，由真推出假，从而既不能为真，也不能为假。*悖论不是命题*\n- 命题符号：\n\t<table>\n<tr>\n<td>命题符号</td>\n<td>意义</td>\n<td>说明</td>\n</tr>\n<tr>\n<td>¬</td>\n<td>否定</td>\n<td></td>\n</tr>\n<tr>\n<td>∨</td>\n<td>析取</td>\n<td>*p* ∨ *q*为真，当且仅当p，q为真</td>\n</tr>\n<tr>\n<td>∧</td>\n<td>合取</td>\n<td>*p* ∧ *q*为假，当且仅当p，q为假</td>\n</tr>\n<tr>\n<td>→</td>\n<td>蕴含</td>\n<td>*p* → *q*为假，当且仅当p为真，q为假</td>\n</tr>\n<tr>\n<td>↔︎</td>\n<td>等价</td>\n<td>*p* ↔︎ *q*为真，当且仅当p，q同时为真或同时为假</td>\n</tr>\n\t</table>\n\t需要说明：\n\t- 自然语言的或有**相容或**和**排斥或**\n\t\t相容或：两者可以同时成立，*p* ∨ *q*\n\t\t排斥或：两者不能同时成立，必须选择其一，(*p* ∨ ¬*q*) ∧ (¬*p* ∨ *q*)\n\t- 蕴含表达式中：p为蕴含式的前件，q为蕴含式的后件。*p* → *q*的意义是q是p的必要条件。\n## 1.2 命题公式及其赋值\n- 简单命题的真值是确定的，又称为命题常项/命题常元，相当于基本数学中的常数。相应的，可以取值为0/1的变元称为**命题变项/命题变元**。*命题变项不是命题*\n- 将命题变项用连结词连接的符号串称为**合式公式**，简称**公式**。若公式B为A中的一部分且B也是公式，则称B为A的子公式。\n\t单个命题变项，¬*A*, (*A* ∧ *B*), (*A* ∨ *B*), (*A* → *B*), (*A* ↔︎ *B*)都是合式公式。\n- 定义中A，B为元语言符号，*A* → *B*或*A* ∨ *B*为对象语言符号。使用元语言符号描述对象语言符号。\n- 设*p*1, *p*2, ...*pn*组成一个合式公式A，对*p*1, *p*2, ...*pn*各指定一个值，这称为A的一个**解释**或**赋值**。若赋值使得A为真，则称为**成真赋值**，反之则为**成假赋值**。\n- 将所有的取值列表，称为真值表。\n- 若A的所有赋值都为真，则称A为**重言式/永真式**。\n\t若A的所有赋值都为假，则称A为**矛盾式/永假式**。\n\t若A不是矛盾式，则称A为**可满足式**。*重言式是可满足式*。\n- 设公式A，B一共含有*p*1, *p*2, ...*pn*，A没有其中一部分，称这部分为**哑元**。由于A的取值和哑元无关，在讨论A，B是否有相同的真值表时，可将A，B看作都含有*p*1, *p*2, ...*pn*。\n# 第2章 命题逻辑等值演算\n## 2.1 等值式\n- 若公式A，B构成的*A* ↔︎ *B*为重言式，则*A* ⇔ *B*，表示A和B是等值的。\n- 若A是重言式，且其中含有*p*1, *p*2...*pn*，将*p*1, *p*2...*pn*替换为其他命题公式，记为B，则B也为重言式。\n\t例如：*A* ⇔ ¬¬*A*可以替换为(*p* ∧ *q*) ⇔ ¬¬(*p* ∧ *q*)\n- 等值式模式：相当于公理，恒成立的公式。\n\t一些有用的模式：\n\t- 分配律<br>\\$\\$<br>A\\\\vee(B\\\\land C) \\\\Leftrightarrow (A\\\\vee B)\\\\land(A\\\\vee C)\\\\\\\\A\\\\land(B\\\\vee C)\\\\Leftrightarrow (A\\\\land B)\\\\vee(A\\\\land C)<br>\\$\\$\n\t- 德摩根律<br>\\$\\$<br>\\\\lnot(A\\\\land B) \\\\Leftrightarrow \\\\lnot A\\\\vee\\\\lnot B\\\\\\\\ \\\\lnot(A\\\\vee B) \\\\Leftrightarrow \\\\lnot A\\\\land\\\\lnot B<br>\\$\\$\n\t- 吸收律<br>\\$\\$<br>A\\\\land(A\\\\vee B)\\\\Leftrightarrow A \\\\\\\\ A\\\\vee(A\\\\land B)\\\\Leftrightarrow A<br>\\$\\$\n\t- 同一律<br>\\$\\$<br>A\\\\land 1 \\\\Leftrightarrow A \\\\\\\\ A\\\\vee 0 \\\\Leftrightarrow A<br>\\$\\$\n\t- 假言易位<br>*A* → *B* ⇔ ¬*B* → ¬*A*\n\t- 蕴含等值式<br>*A* → *B* ⇔ ¬*A* ∨ *B*\n\t- 归谬<br>(*A* → *B*) ∧ (*A* → ¬*B*) ⇔ ¬*A*\n\t将A，B代入以上模式，这些具体的等值式称为等值式的**代入实例**。\n- 置换规则：若*A* ⇔ *B*设*f*(*A*)是包含A的公式，将B替换A称为*f*(*B*)，则*f*(*A*) ⇔ *f*(*B*)。\n## 2.2 合取范式与析取范式\n- 命题变项/命题变项的否定称为**文字**，有限个文字析取式称为**简单析取式**，有限个文字合取式成为**简单合取式**\n- 定理：若析取式中含p和非p，A必然重言。如果A重演，则该析取式中必然有p和非p。若合取式中含p和非p，A必然矛盾。如果A矛盾， 则该合取式中必然有p和非p。\n- 由有限个简单合取式析取构成的公式称为**析取范式**，由有限个简单析取式合取构成的公式称为**合取范式**。\n- *每个公式都有与其等价的合取范式和析取范式。*将一般公式转化为合取/析取范式时，由于范式只含有∧∨，所以需要转化¬ → ↔︎连接符。\n\t- 遇到→：使用蕴含等值式：*A* → *B* ⇔ ¬*A* ∨ *B*\n\t- 遇到↔︎：使用等价等值式：*A* ↔︎ *B* ⇔ (*A* → *B*) ∧ (*B* → *A*)\n\t- 不出现¬¬*A*：使用双重否定律。\n\t- 不出现¬(*A* ∨ *B*), ¬(*A* ∧ *B*)：使用德摩根律：¬(*A* ∨ *B*) ⇔ ¬*A* ∧ ¬*B*\n\t- 合取范式中不出现*A* ∨ (*B* ∧ *C*)，析取范式中不出现*A* ∧ (*B* ∨ *C*)：使用分配律。\n- **极小/大项**：公式A中所有的命题变项或其否定都只出现一次且按字典序排序，组成的简单合取/析取式。\n\t所有简单合取/析取式都是极小/极大项的析取/合取范式称作**主析取/合取范式**\n# 第3章 命题逻辑的推理理论\n## 3.1 推理的形式构成\n- **推理**是从前提出发推出结论的过程。**前提**是已知的命题公式集合，**结论**是从前提出发推出的命题公式。\n- 对于前提R，使得蕴含式*R* → *B*为重言式，则由前提R推出结论B的推理是有效的或正确的。B为有效的结论。\n- 一些重要的推理定律：<br>\\$\\$<br>附加律：A\\\\Rightarrow\\\\left(A\\\\vee B\\\\right) \\\\\\\\<br>化简律：\\\\left(A\\\\land B\\\\right)\\\\Rightarrow A \\\\\\\\<br>假言推理：\\\\left(A\\\\rightarrow B\\\\right)\\\\land A\\\\Rightarrow B \\\\\\\\<br>拒取式：\\\\left(A\\\\rightarrow B\\\\right)\\\\land\\\\lnot B\\\\Rightarrow\\\\lnot A \\\\\\\\<br>析取三段论：\\\\left(A\\\\vee B\\\\right)\\\\land\\\\lnot B\\\\Rightarrow\\\\lnot A \\\\\\\\<br>假言三段论：\\\\left(A\\\\rightarrow B\\\\right)\\\\vee\\\\left(B\\\\rightarrow C\\\\right)\\\\Rightarrow\\\\left(A\\\\rightarrow C\\\\right) \\\\\\\\<br>构造性二难：\\\\left(A\\\\rightarrow B\\\\right)\\\\land\\\\left(C\\\\rightarrow D\\\\right)\\\\land\\\\left(A\\\\vee C\\\\right)\\\\Rightarrow\\\\left(B\\\\vee D\\\\right) \\\\\\\\<br>构造性二难（特殊形式）：\\\\left(A\\\\rightarrow B\\\\right)\\\\land\\\\left(\\\\lnot A\\\\rightarrow B\\\\right)\\\\Rightarrow B \\\\\\\\<br>破坏性二难\\\\left(A\\\\rightarrow B\\\\right)\\\\land\\\\left(C\\\\rightarrow D\\\\right)\\\\land\\\\left(\\\\lnot B\\\\vee\\\\lnot D\\\\right)\\\\Rightarrow\\\\left(\\\\lnot A\\\\vee\\\\lnot C\\\\right)<br>\\$\\$\n## 3.2 自然推理系统P\n- 使用条件中的公式推理得到结论B，称为证明。每个条件之间是合取关系，所以与引入顺序无关。\n- 在证明题中，题目通常如下出现：\n\t前提：……<br>结论：……<br>证明：\n- 附加前提证明法\n\t例如需要证明的结论为*s* → *q*，那么假设s成立，将其放置于条件中，只需要证明结论s成立即可。\n# 第4章 一阶逻辑基本概念\n命题逻辑具有一定局限性，我们引入量词概念，以期表达出个体和总体之间的内在联系和数量关系。\n## 4.1 一阶逻辑命题符号化\n个体词，谓词，量词是一阶逻辑命题符号化的3个基本要素。\n- 个体词\n\t个体词是指研究对象中可以独立存在的客体，他们不受命题约束。表示具体或特定客体的个体词称为**个体常项**，用小写字母a，b，c表示。将抽象的，泛指的个体词称为**个体变项**，用x，y，z表示。个体变项的取值范围为**个体域**，如自然数集合N，实数集合R。有一个特殊的个体域，包含了宇宙中所有的事物，称为**全总个体域**，如果没有特殊说明，默认使用全总个体域。\n- 谓词\n\t谓词是用来刻划个体词性质和其之间关系的词。通常用大写字母F,G,H表示。如F(x)可以表示x是常数，G(x, y)可以表示x\\<y。\n\t表示具体事物具体关系的谓词称为**谓词常项**，如F(a)：a是理塘丁真。表示泛指或抽象事物的谓词称为**谓词变项**，上面的F(x)和G(x, y)都是谓词变项。\n- 量词\n\t量词分为两种：**全程量词**和**存在量词**。我们平时说的*凡是，每一个，任意的……*都是全程量词，记作∀。例如∀*x*∀*yF*(*x*, *y*)可以表示对于任何的x和y，都满足x\\<y。而说的*存在，有一个，至少有一个*都是存在量词，记作\\$\\\\exist\\$。\n- 特征谓词：当使用全总个体域时，为了将一样物体与其他物体区分开来，引进的谓词。\n## 4.2 一阶逻辑公式及其解释\n- 一阶语言是用于一阶逻辑的形式语言，可以看成命题公式加入了谓词之后的结果。\n- 对于一个一阶逻辑公式L，命题常项、函数符号、谓词符号称为非逻辑符号，命题变项、量词、连结词，括号与逗号称为逻辑符号。*注意命题变项是逻辑符号*\n- 项：个体常项，变项与函数的结合，可以看成单项式。\n- 原子公式：单个谓词符号。\n- 合式公式：数个谓词符号相结合。\n其实这些定义和第一、二章公式的概念非常类似，无非就是加上了“谓词”和“量词”这两个玩意。下面是一些零碎的定义。\n- 在公式∀*xA*和公式∃*xA*中，x称为指导变元，公式A称为x的辖域。在辖域中出现的x称为**约束出现**，而其他变元称为**自由出现**。\n\t使用*Δx*表示x受约束（∀*x*/∃*x*），在*Δx*1*A*(*x*1, *x*2)中，x2可以自由出现。\n- 如果A中不含有任何自由出现的命题变项，称A为**封闭的公式**，简称**闭式**\n- 对公式中个体域，个体常项，函数符号，谓词的指定称为**解释**。对自由出现的变项的指定称为**赋值**\n- 将公式A中个体域D指定为D1；出现的个体常项a替换为固定的a拔；函数f替换为固定的f拔；谓词F替换为固定的F拔；个体变项x指定为D1中出现的一个值，这样的公式值为A‘，是A的一个解释，或称为A被解释为A’。\n\t*这个东西绝壁会考*\n- 设A在任何解释下为真，则A为永真式。任何解释下为假，则A为永假式。若A可以为真，则称可满足式。（与公式那一块一样。）\n- 将其他公式代入A，称为A的代换实例。\n# 第5章 一阶逻辑等值演算与推理\n## 5-1 一阶逻辑等值式和量词逻辑\n- 对于一阶逻辑中的两个公式A，B，如果A蕴含B为永真式，则称A与B等值。\n- 下面是一阶逻辑中的基本等值式。\n\t- 量词否定等值式<br>\\$\\$<br>\\\\lnot \\\\forall xF(x) \\\\Leftrightarrow \\\\exists x \\\\lnot F(x)\\\\\\\\<br>\\\\lnot \\\\exists xF(x) \\\\Leftrightarrow \\\\forall x \\\\lnot F(x)<br>\\$\\$<br>这个公式的直观理解是：不是所有的x满足F(x) 和 存在x不满足F(x) 是等效的。\n\t- 量词辖域收缩与扩张<br>\\$\\$<br>\\\\forall\\\\left(A\\\\left(x\\\\right)\\\\vee B\\\\right)\\\\Leftrightarrow\\\\forall xA\\\\left(x\\\\right)\\\\vee B \\\\\\\\<br>\\\\forall\\\\left(A\\\\left(x\\\\right)\\\\land B\\\\right)\\\\Leftrightarrow\\\\forall xA\\\\left(x\\\\right)\\\\land B \\\\\\\\<br>\\\\forall\\\\left(A\\\\left(x\\\\right)\\\\to B\\\\right)\\\\Leftrightarrow\\\\exists xA\\\\left(x\\\\right)\\\\to B \\\\\\\\<br>\\\\forall(B \\\\to A(x))\\\\Leftrightarrow B \\\\to \\\\forall xA(x) \\\\\\\\<br>\\$\\$<br>存在量词同理，除第三个为：<br>∃(*A*(*x*) → *B*) ⇔ ∀*xA*(*x*) → *B*\n\t- 量词分配等值式<br>\\$\\$<br>\\\\forall x(F(x) \\\\land G(x)) \\\\Leftrightarrow \\\\forall xF(x) \\\\land \\\\forall xG(x) \\\\\\\\<br>\\\\exists x(F(x) \\\\vee G(x)) \\\\Leftrightarrow \\\\exists xF(x) \\\\vee \\\\exists xG(x)<br>\\$\\$<br>注意任意量词对合取有分配律，存在量词对析取有分配律\n\t- 换名规则\n\t\t在某量词辖域内，将一个约束出现的指导变元及其出现全部变为该量词辖域内另一个没有出现过的个体变项符号，换名前后公式等效。\n\t\t例如：<br>∀*x*(*F*(*x*, *y*) → ∃*yG*(*x*, *y*, *z*))<br>等价于：<br>∀*x*(*F*(*x*, *y*) → ∃*tG*(*x*, *t*, *z*))<br>其实是因为两个谓词中两个个体变项即使符号相同，意义却不同，可以任意替换。\n- 当个体域为有限集时，可以消去量词。<br>\\$\\$<br>D = \\\\\\{ a, b, c...z\\\\\\}\\\\\\\\<br>\\\\exists xF(x) \\\\Leftrightarrow F(a) \\\\vee F(b) \\\\vee F(c) ... \\\\vee F(z)\\\\\\\\<br>\\\\forall xF(x) \\\\Leftrightarrow F(a) \\\\land F(b) \\\\land F(C)... \\\\land F(z)<br>\\$\\$\n\t感觉要考。\n## 5-2 一阶逻辑的前束范式\n- 具有*Q*1*x*1, *Q*2*x*2...*QkxkB*的一阶逻辑公式称为其前束范式，其中*Q*1, *Q*2....*Qk*为量词，B为不含量词的公式。\n\t**前束范式存在定理**：一阶逻辑公式都存在等值的前束范式，且*不唯一*。",
  "离散数学：集合代数": "> 恭喜你完成了数理逻辑的学习，现在迎接你的是更大的一坨：集合论。记得高中学过的集合吧。对，跟那玩意没多大关系，你很快就会知道了。\n---\n# 第6章 集合代数\n## 6-1 集合的基本概念\n- 集合这个概念无法被精确定义。它将一系列具有相同属性的元素汇集成为一个整体。这些事物是这个集合的**元素**或**成员**。\n\t表示集合有两种方法：列元素法和谓词表示法。\n\t集合的元素*不重复*，且集合是*无序*的\n- 设A，B为集合，若B中的每个元素都是A的元素，称B是A的子集，称B被A包含，或A包含B，记作*B* ⊆ *A*（不被包含：*B*⊈A）。\n- 如果*A* ⊆ *B*且*B* ⊆ *A*，则A=B，称A，B相等。\n- 如果*B* ⊆ *A*且*B* ≠ *A*，称B是A的真子集，记作*B* ⊂ *A*。\n- 空集∅不包含任何元素，是任何元素的子集，且*唯一*。\n- 含有n个元素的集合称为n元集。\n- A的全体子集构成的集合称为A的幂集，记作*P*(*A*)。若A是n元集，则P(A)有2*n*个元素。\n## 6-2 集合的运算\n- 并集*A* ∪ *B*、交集*A* ∩ *B*\n\t相对补集：*A* − *B*，即A中有但B中没有的符号，可以理解为减号。\n\t绝对差集：*A* ⊕ *B*，AB并集减去AB交集，即两个相对补集的并集 。\n\t绝对补集：\\~*A*，给定全集E，绝对补集就是E对A的相对补集。\n- 广义交：∩*A*，A中所有元素的公共元素构成的集合，例如：<br>\\$\\$<br>A=\\\\\\{a, \\\\\\{c, d\\\\\\},\\\\\\{c\\\\\\}\\\\\\}\\\\\\\\<br>\\\\cap A = a\\\\cap\\\\\\{c\\\\\\}<br>\\$\\$\n- 广义并：∪*A*，如果A的元素是集合，广义并A是A中集合元素之和。\n- 空集的广义并仍是空集。空集不能进行广义交。\n- 广义交，广义并，*P*(*A*)的优先级大于∩, ∪, −, ⊕。\n## 6-3 有穷集的计数\n- 集合的关系和初级运算可以通过文氏图表现。\n- 有穷集的计数问题可以通过包含排斥原理求解。具体过程请看书。\n## 6-4 集合恒等式\n和公式、一阶逻辑一样，集合也有用于逻辑推理的恒等式。下面的恒等式列出了集合运算的主要算律。\n- 幂等律<br>\\$\\$<br>A \\\\cap A = A\\\\\\\\<br>A \\\\cup A = A<br>\\$\\$\n- 结合律，交换律\n- 分配律<br>\\$\\$<br>A \\\\cap (B \\\\cup C) = (A\\\\cap B) \\\\cup (A\\\\cap C)\\\\\\\\<br>A \\\\cup (B \\\\cap C) = (A\\\\cup B) \\\\cap (A\\\\cup C)<br>\\$\\$\n- 同一律<br>\\$\\$<br>A \\\\cap E = A \\\\\\\\<br>A \\\\cup \\\\emptyset = A<br>\\$\\$\n- 零律<br>\\$\\$<br>A \\\\cap \\\\emptyset = \\\\emptyset\\\\\\\\<br>A \\\\cup E = E<br>\\$\\$\n- 排中律\n- 矛盾律\n- 吸收律<br>\\$\\$<br>A \\\\cap(A \\\\cup B) = A\\\\\\\\<br>A \\\\cup(A \\\\cap B) = A<br>\\$\\$\n- 德摩根律\n\t> 由于没找到\\~对应的公式符号，而且直接打的话会消失，所以排中律，矛盾律和德摩根律请至书中101页\n# 第7章 二元关系\n## 7-1 有序对与笛卡尔积\n- 将两个元素x和y按照一定顺序排列成的二元组称作一个**有序对**，记作\\<x,y\\>，其中x是它的第一元素，y是第二元素。将x，y颠倒组成的新有序对与原来的不相等。\n- 设A，B是集合，用A中的元素为第一元素，B中的元素为第二元素组成的有序对称作**笛卡尔积**，记作*A* × *B*。\n- 对于一般的集合，空集和A的笛卡尔积仍未空集。\n\t一般来说，笛卡尔积不满足交换律，不满足结合律，但满足交换律。\n## 7-2 二元关系\n- 将一个元素都是有序对的非空集合或空集称为**二元关系**（简称**关系**）。记作*R*0，如果 \\< *x*, *y* \\> ∈ *R*则有*xRy*。\n- 设A，B为集合，则AxB的任意子集所定义的二元关系称作从A到B的**二元关系**。特别地当A=B时称为A上的二元关系，∅称为A的空关系。\n- **全域关系***EA*是指AxA中除却空集外的所有关系的集合。\n\t如A=\\{ 1, 2 \\}，*EA* = \\{ \\< 1, 1\\>, \\< 1, 2\\>, \\< 2, 1\\>, \\< 2, 2 \\> \\}\n- **恒等关系***IA*是A中单个元素组成的关系，如*IA* = \\{ \\< 1, 1\\>, \\< 2, 2 \\> \\}\n- **小于等于关系***LA*是\\<x, y\\>且x≤y。\n- **整除关系***DA*是\\<x, y\\>，且y能被x整除。\n- 设R的**关系矩阵**的第i，j元素为0时xi，xj不满足R关系；为1时满足R关系。\n- R的关系图记作*GR*。如果*xiRxj*成立，就有一条线从xi指向xj。\n## 7-3 关系的运算\n- R中有序对第一元素的集合为R的**定义域**，记作*domR*。\n\tR中有序对第二元素的集合为R的**值域**，记作*ramR*。\n\t定义域和值域的并集称为R的域，记作*fldR*。\n- R的**逆关系**为*R*−1，就是将x，y颠倒位置。 \\< *x*, *y* \\> → \\< *y*, *x*\\>。\n- 设F，G为二元关系，G对F的**右复合**记作*F* ∘ *G*。有点像合并两个二元关系 \\< 1, 2 \\> \\< 2, 3 \\> → \\< 1, 3\\>。\n\t用符号表示为：<br>*F* ∘ *G* = \\{ \\< *x*, *y* \\> \\| ∃*t*( \\< *x*, *t* \\>  ∈ *F* ∧ \\< *t*, *y* \\>  ∈ *G*)\\}<br>亦可以定义左复合，不做赘述。\n- R在A的**限制**，记作*R* ↑ *A*，指R中\\<x, y\\>且x在集合A中。\n- R在A的**像**，记作*R*\\[*A*\\]，即*ran*(*R* ↑ *A*)，就是上面定义中的y。\n⊆  ⊈  ⊂  ⊂ ∅ ∪ ∩ ⊕ × ∈ *F* ∘ *G*↑",
  "大学物理：近代物理": "在学习完光学之后，我们也正式跨入了近代物理的学习。在这一章节中，我们将了解诸如爱因斯坦等老登推翻了经典物理的时空观，开创了相对时空观念，在物理学的历史上划上了浓墨重彩的一笔。\n---\n# 第十四章 相对论\n## 14-1 经典力学的绝对时空观\n经典力学认为空间只是物质运动的场所，是与其中的物质相独立的存在，它不会影响其中的物质，且是永恒不变，永远静止的。这也就意味着，如长度这些空间的度量是永恒不变的。例如，经典物理认为，一把长度为10cm的尺子，在任何情况下，长度都为10cm，这不会随着惯性系的变化而变化。\n另外，经典物理还认为，时间也和运动无关。它是一种度量，永恒且均匀地流动。对于不同的参考系，时间是绝对的。如果一个事件发生了，在另一个惯性系看来，它是同时发生的。\n## 14-2 没必要看\n## 14-3 狭义相对论，洛伦兹变换式\n### 狭义相对论的基本原理\n爱因斯坦提出了狭义相对论，它认为时间和空间不再是绝对的，静止的，它们也会随着惯性系的变化而变化。\n- 爱因斯坦相对论原理：物理定律在所有的惯性系中都具有相同的表达形式，即所有的惯性参考系对运动的描述都是等效的。\n\t上面的论述比较专业，当然也很难看懂。简而言之，这意味着绝对静止的参考系不存在（推翻了经典物理的时空是绝对的），一切运动都是相对概念。\n- 光速不变原理：真空光速为常量，它与光源或观测者的运动无关，即与惯性系的选择无关。\n\t举个例子，你从一辆速度为v的车上向前抛出一个罐头，那么这个罐头将以v+v‘运动；但如果你向前射出一束光，这束光将以v光运动，而不是v+v光。*我记得这是我很早之前看过的一部纪录片里面讲的。*\n### 洛伦兹变换式\n对于两个相对运动的惯性参考系，其位置x和时间t具有以下关系：<br>\\$\\$<br>x' = \\\\frac\\{x-vt\\}\\{\\\\sqrt\\{1-\\\\beta\\^2\\}\\} \\\\\\\\ t'=\\\\frac\\{t-\\\\frac\\{v\\}\\{c\\^2\\}x\\}\\{\\\\sqrt\\{1-\\\\beta\\^2\\}\\}<br>\\$\\$<br>其中β指的是v/c，一般来说题目给的速度v都是c的形式（如1/3c），这样是为了简化计算。\n## 14-4 狭义相对论的时空观\n### 同时的相对性\n狭义相对论认为，在惯性系S中某两个现象被同时观测到，但在另一个参考系S’中，这两个现象不一定被同时观测到。\n如果一列火车的前后都有一盏灯，对于在火车外的参考系S，这两盏灯同时打开；但是对于在火车内的参考系S‘，由于火车向前运动而光的传播需要时间，实际上观测到车前的灯比车后的灯更早亮起。\n### 长度的收缩\n根据洛伦兹变换式<br>\\$\\$<br>x' = \\\\frac\\{x-vt\\}\\{\\\\sqrt\\{1-\\\\beta\\^2\\}\\}<br>\\$\\$<br>S参考系中相距Δx的两个**同时**发生的事件，在S’中相距Δx‘满足<br>\\$\\$<br>\\\\Delta x'=\\\\Delta x\\\\sqrt\\{1-\\\\beta\\^2\\}<br>\\$\\$<br>或者<br>\\$\\$<br>l'=l_0\\\\sqrt\\{1-\\\\beta\\^2\\}<br>\\$\\$<br>我们把S参考系中测量出的长度称为**固有长度**，我将该系称作**固有系**；相应将S‘称作**观测系**。在题目当中，事件实际发生的参考系为固有系，观测事件发生的参考系为观测系。\n由上面的公式可以知道：由于β\\^2大于等于零，所以l’一定是小于等于l0的，也就意味着只要两个参考系有相对运动，测量出的长度一定是小于其固有长度的。物体的这种沿运动方向收缩的现象称作**洛伦兹收缩**，上面的公式称作**尺缩公式**。\n### 时间的延缓\n在狭义相对论中，如长度不是绝对的一样，时间也不是绝对的。在S参考系测量出的时间和S‘参考系测量出的时间不同。根据洛伦兹变换式<br>\\$\\$<br>t'=\\\\frac\\{t-\\\\frac\\{v\\}\\{c\\^2\\}x\\}\\{\\\\sqrt\\{1-\\\\beta\\^2\\}\\}<br>\\$\\$<br>S参考系中发生时间间隔Δt的两个发生在同一地点的事件，在S’中Δt‘满足<br>\\$\\$<br>\\\\Delta t' = \\\\frac\\{\\\\Delta t\\}\\{\\\\sqrt\\{1-\\\\beta\\^2\\}\\}<br>\\$\\$<br>我们把S参考系中测出的时间间隔Δt称为**固有时**，S’参考系中测出的时间间隔Δt‘称为**运动时**。这样的效应称为**时间延缓**效应。\n## 14-5 相对论性动量和能量\n### 狭义相对论的动量方程\n在狭义相对论中，惯性系间速度变化满足洛伦兹变换式，则动量表达式应为：<br>\\$\\$<br>p=\\\\frac\\{m_0 v\\}\\{\\\\sqrt\\{1-v\\^2/c\\^2\\}\\}<br>\\$\\$<br>该形式称为**相对论性动量表达式**。为了保持P=mv的形式，该式也写成*p* = *mv*，但其中\\$m=\\\\frac\\{m_0\\}\\{\\\\sqrt\\{1-v\\^2/c\\^2\\}\\}\\$，称为相对论质量；原来的m0则是在质点相对惯性系静止时的质量，称为静质量。\n### 狭义相对论力学的基本方程\n对于外力F，有\\$F=\\\\frac\\{d\\}\\{dt\\}\\\\frac\\{m_0 v\\}\\{\\\\sqrt\\{1-v\\^2/c\\^2\\}\\}\\$，其实就是经典力学中的公式加上\\$\\\\frac\\{1\\}\\{\\\\sqrt\\{1-v\\^2/c\\^2\\}\\}\\$即可。\n### 狭义相对论的质能关系\n当物质运动时，有*Ek* = *mc*2 − *moc*2。这是**相对论动能公式**，看上去和经典力学中的公式没什么相似之处（也只是看上去）。\n将此式变换，可以得到*mc*2 = *Ek* + *m*0*c*2。这说明了物体的总能量是物体的静止能量和动能量组成的。如果以E指代物体的总能量，则有*E* = *mc*2，这便是大名鼎鼎的**质能转换方程**。对于变化的m，也有变化的E，满足：*ΔE* = *Δmc*2。\n动量和能量还有一种很便于使用的公式：*E*2 = *Ek*2 + *p*2*c*2。请记住和动能公式*E* = *Ek* + *m*0*c*2区分。这两个公式组合可以求出很多东西（如动量p）。\n**请注意，在相对论公式中出现的m都是相对论质量，而并非m0。**\n# 第十五章 量子物理\n## 15-1 黑体辐射，普朗克能量子假设\n### 黑体，黑体辐射\n任何物体都在无时不刻向外辐射和吸收电磁波。设想一种物质，它会吸收所有入射的电磁波（而不反射回去），这种物质称为黑体。绝对黑体是一种理想模型。黑体仅仅是将入射的电磁波全部吸收，这并不意味着它不会向外辐射电磁波。事实上，黑体向外辐射的电磁波是一个有关热力学温度T的函数。\n该辐射用以下量表示：\n- 单色辐出度\n\t从热力学温度为T的黑体的的单位面积上，单位时间内从波长为λ附近辐射出的电磁波能量。它是λ和T的函数，记作*Mλ*(*T*)。\n- 辐出度\n\t一个黑体各种波长的单色辐出度的综合，记作*M*(*T*)。很显然是*Mλ*(*T*)的积分：<br>\\$\\$<br>M(T)=\\\\int\\^\\\\infin_0\\{M_\\\\lambda(T)d\\\\lambda\\}<br>\\$\\$\n### 斯特藩-玻尔兹曼定律 维恩位移定律\n- 斯特藩-玻尔兹曼定律\n\t黑体辐出度M(T)与黑体的热力学温度T的四次方成正比。<br>*M*(*T*) = *σT*4<br>该式称为斯特藩-玻尔兹曼定律。其中σ是斯特藩-玻尔兹曼常量，值为5.670 × 10−8*W* · *m*−2 · *K*−4。\n- 维恩位移定律：黑体辐射的峰值波长λm与T相乘是个常量，即：<br>*λmT* = *b*<br>当T升高时，λm会向短波长移动。这即为维恩位移定律。",
  "大学物理：光学": "> 我真的很想跟大学物理，离散数学，数据结构一起爆了。愿每个学习大学物理的都有美好的未来。\n\t正经人谁会把大学物理的笔记写在博客上面？\n有些东西不写下来，时间长了是真记不住。程序设计语言如此，大学物理亦是如此。鉴于纸质笔记不方便携带，而且排版很麻烦，还是用md写好了。\n---\n# 第十一章：光学\n## 11-1 相干光\n- 从两个不同的光源发出的光很难构成相干光，需要使用一些方法。\n## 11-2 杨氏双缝干涉\n杨氏双缝干涉是将光源经过双缝过滤成为两束相干光，使得这两束相干光发生干涉。\n### 杨氏双缝干涉判断加强减弱点的判断条件\nx：光屏上的点到原点的距离\nd：双缝间距\nd‘：双缝到光屏距离\n若满足：<br>\\$\\$<br>x=±\\\\frac\\{d'\\}\\{d\\} kλ<br>\\$\\$<br>则为加强点。<br>\\$\\$<br>x=±\\\\frac\\{d'\\}\\{d\\}(2k+1)\\\\frac\\{\\\\lambda\\}\\{2\\}<br>\\$\\$<br>则为减弱点。\n光屏上相邻明暗条纹间距为：<br>\\$\\$<br>\\\\frac\\{d'\\}\\{d\\} \\\\lambda<br>\\$\\$\n### 光程和光程差\n当两束相干光在同一个介质中传播时，只需要将几何路径相减即可算出相位差。但如果两束光不在同一种介质中，由于传播速度的变化，无法使用几何路径差直接计算相位差。故引入光程和光程差的概念。<br>*光程* = *折射率n* × *传播几何路径长度l*<br>光程差（记为Δ）与相位差的关系为：<br>\\$\\$<br>Δ\\\\varphi = 2\\\\pi\\\\frac\\{Δ\\}\\{\\\\lambda\\}<br>\\$\\$<br>所以当<br>*Δ* = ±*kλ*<br>时干涉加强。当<br>\\$\\$<br>Δ=\\\\pm (2k+1)\\\\frac\\{\\\\lambda\\}\\{2\\}<br>\\$\\$<br>时干涉减弱。\n## 11-3 薄膜干涉\n假设照射一束光波于薄膜，由于折射率不同，光波会被薄膜的上界面与下界面分别反射，因相互干涉而形成新的光波，这现象称为薄膜干涉。\n### 薄膜干涉加强减弱的条件\nd：薄膜厚度\ni：入射角度（与垂直方向）\nn1，n2：空气折射率，薄膜折射率<br>\\$\\$<br>Δr=2d\\\\sqrt\\{n_2\\^2 - n_1\\^2\\\\sin i\\} + \\\\frac\\{\\\\lambda\\}\\{2\\}<br>\\$\\$<br>等于kλ/2时加强，等于(2k+1)λ/2时减弱。\n- **特殊情况：当光垂直入射时**\n\t当光垂直入射时，即sin(i) = 0，有：<br>\\$\\$<br>Δr=2dn_2+\\\\frac\\{\\\\lambda\\}\\{2\\}<br>\\$\\$<br>等于kλ/2时加强，等于(2k+1)λ/2时减弱。\n### 相位跃变\n相位跃变是指光从低折射率（高光速）介质射向高折射率（低光速）介质时，产生的反射光的相位突变，使得光程差增加了λ/2（就是上面的）。\n### 增反膜与增透膜\n增透膜：增加透射光，即在膜的两侧，反射光干涉相消（减弱）。\n增反膜：增加反射光，即在膜的两侧，反射光干涉相长（增强）。\n## 11-4 劈尖，牛顿环，迈克耳孙干涉仪\n### 劈尖\n光线在射入劈尖时，在空气层的上层发生反射（没有相位跃变），在空气层的底部发生反射（发生相位跃变），所以光程差为：<br>\\$\\$<br>Δ=2nd+\\\\frac\\{\\\\lambda\\}\\{2\\}<br>\\$\\$<br>当<br>\\$\\$<br>Δ=2nd+\\\\frac\\{\\\\lambda\\}\\{2\\}=\\\\pm k\\\\lambda<br>\\$\\$<br>时干涉加强。当<br>\\$\\$<br>Δ=2nd+\\\\frac\\{\\\\lambda\\}\\{2\\}=\\\\pm (2k+1)\\\\frac\\{\\\\lambda\\}\\{2\\}<br>\\$\\$<br>时干涉减弱。\n- 劈尖干涉又称等厚干涉，产生的干涉条纹称作等厚干涉条纹。\n在已知波长的情况下，可以求出两条纹间的劈尖厚度差为：<br>\\$\\$<br>Δd=\\\\frac\\{\\\\lambda\\}\\{2n\\}<br>\\$\\$<br>由于劈尖夹角很小，tanθ≈θ，所以有：<br>\\$\\$<br>θ=\\\\frac\\{D\\}\\{L\\}=\\\\frac\\{\\\\lambda\\}\\{2b\\}<br>\\$\\$<br>其中D为劈尖高度，L为劈尖长度。\n### 牛顿环\n明环半径：<br>\\$\\$<br>r=\\\\sqrt\\{(k - \\\\frac\\{1\\}\\{2\\})\\\\frac\\{R\\\\lambda\\}\\{n\\}\\}<br>\\$\\$<br>暗环半径：<br>\\$\\$<br>r=\\\\sqrt\\{\\\\frac\\{KR\\\\lambda\\}\\{n\\}\\}<br>\\$\\$\n## 11-5 光的衍射\n没什么好说的\n## 11-6 夫琅和费单缝衍射\n在光学上，夫琅和费衍射（以约瑟夫·冯·夫琅和费命名），又称远场衍射，是波动衍射的一种，在场波通过圆孔或狭缝时发生，导致观测到的成像大小有所改变，成因是观测点的远场位置，及通过圆孔向外的衍射波有渐趋平面波的性质。\n### 夫琅和费加强减弱条件\nb：单缝宽度\nθ：光路与狭缝所成角度\n当满足：<br>\\$\\$<br>b\\\\sinθ=\\\\pm k\\\\lambda<br>\\$\\$\n时减弱，当满足：<br>\\$\\$<br>b\\\\sinθ=\\\\pm (2k+1)\\\\frac\\{\\\\lambda\\}\\{2\\}<br>\\$\\$<br>时增强。\n**一定注意夫琅和费的条件和杨氏双缝是正好相反的。**\n## 11-7 光学仪器的分辨\n当入射光偏移过大时，由于衍射和干涉，光学仪器将变得难以分辨。光学仪器的最小分辨角为：\nθ：最小分辨角\nλ：光波长\nD：光学仪器的透光孔径<br>\\$\\$<br>\\\\theta=1.22\\\\frac\\{\\\\lambda\\}\\{D\\}<br>\\$\\$<br>将其倒数1/θ称作光学仪器的分辨本领。\n## 11-8 衍射光栅\n在玻璃板上刻出条纹，刻纹处不透光，刻纹之间区域透光，相当于单缝，这即是平面衍射光栅。\n> 还有一种在铝板上刻划一系列等间距的平行槽痕，这种光栅也叫做闪耀光栅。闪耀，优骏光栅！\n### 光栅常量\n设不透光部分长度为b，透光部分长度为b‘，则<br>*d* = *b*′ + *b*<br>为光栅的光栅常量。\n> 一般光栅常量数量级为10-6 \\~ 10-5m\n### 光栅方程\n光栅的衍射条件是干涉和衍射的总结果。\n公式<br>(*b* + *b*′)sin *θ* = ±*kλ*<br>称为光栅方程。k=0的条纹称为中央明纹。\n## 11-9 光的偏振，马吕斯定律\n### 偏振光\n光可以包含在任何方向上传播的振动。如果一束光在各个方向的振动几乎相同，没有一个方向上占优势，则称为**自然光**，使用密度相等的竖线和圆点表示，竖线代表光的振动方向平行于纸面，圆点代表振动方向垂直于纸面。\n振动只在一个方向上的光称为**线偏振光**，简称偏振光。偏振光振动方向与传播方向形成的平面称作偏振面。各个方向振动都有，但振幅不等的光，称为部分偏振光。\n一些物质可以吸收一个方向上的光，用这种特殊的物质涂抹在玻璃上，即可制成偏振片。这个特定的方向称为**偏振化方向**，记作↕。使自然光发生偏振的装置称作起偏器，起偏器也能作为检偏器。\n### 马吕斯定律\n偏振光的光强I与偏振片和检偏器的夹角之间的关系：<br>*I* = *I*0cos2*α*\n## 11-10 反射光的偏振\n当自然光入射到介质中时，反射光和折射光都是部分偏振光。反射光中垂直于截面的光多，折射光中平行于截面的光多。\n当入射角满足：<br>\\$\\$<br>\\\\tan\\\\theta = \\\\frac\\{n_1\\}\\{n_2\\}<br>\\$\\$<br>时，反射角为纯偏振光。该角称为起偏角或布儒斯特角，该定律称为布儒斯特定律。\n## 11-11 双折射\n当光入射到某些介质（如方解石晶体中）时，会折射出两束光线，一道光线遵循折射定律，称为寻常光线（o光）；一道光线不遵从折射定律，其介质光速因入射方向变化而变化，称为非寻常光线（e光）。这两束光都是偏振光。\n两束光只在介质内有意义，射出介质后，这两束光都是普通的偏振光。"
};

